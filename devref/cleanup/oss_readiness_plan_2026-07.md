# BQuant — План приведения пакета к OSS-состоянию (мастер-документ)

**Дата создания:** 2026-07-20
**Тип:** стратегия + дорожная карта (живой документ)
**Статус:** 🟢 согласовано в обсуждении; исполняем мелкими шагами
**Спутники:** детальный реестр — `devref/cleanup/package_drying_inventory_2026-07-20.md`;
предшественник — `devref/architecture/revival_plan_2026-07.md` (пересмотрен, см. §1).

> **Как пользоваться этим документом.** Это опорная карта. Работаем **мелкими шагами**:
> один маленький шаг → обсуждение и полное понимание → исполнение → ревью → следующий.
> Ничего необратимого без явного согласия. Прогресс пишем в §7.

---

## 1. Рамка и что изменилось (пересмотр revival-плана)

`revival_plan_2026-07.md` откладывал уборку, сплит и англ-версию — потому что тогда целью
была **петля рисёрча** внутри одного репо. С тех пор произошёл **сплит**: аналитика и поиск
альфы переехали в приватную лабу `bquearch`. Поэтому:

- **`bquant` теперь = публичный тулкит.** Его задача — быть чистым, «сухим», пригодным для OSS
  и для ИИ-CLI пакетом. Рисёрч/методология живут в лабе.
- Ограничения revival-плана под эту новую цель **пересмотрены** (уборка из «отложить» →
  в «делать»). Anti-todo частично снят (см. §4).
- Зависимость по-прежнему строго `bquearch → bquant`. Пакет ничего не знает про лабу.

## 2. North-star (куда идём)

**Пакет как чистое ядро + документация как отдельный продукт с несколькими интерфейсами.**

Целевые интерфейсы доки (не сейчас, но как ориентир): **ReadTheDocs · свой сайт ·
AI-friendly дока** (напр. `llms.txt`/чанкуемый markdown/машиночитаемый API-reference).

Ключевой принцип, делающий это дёшевым: **один источник истины → много рендер-таргетов.**
RTD, сайт и AI-render — это три сборки над одним чистым, оттестированным источником, а не три
разных дока. Отсюда — **исходник доков остаётся в пакете** (рядом с кодом, для парити), а
продуктом становится **витрина/сайт**, не исходник.

**Дальний прицел — RAG-на-минималках для ИИ-CLI:**
- **Дока = семантический слой:** что делает, зачем, как применять.
- **Код-граф = структурный слой:** что от чего зависит, что откуда течёт, кого заденет правка.
- Вместе они закрывают то, на чём ИИ обычно галлюцинирует (дока не знает структуру, греп не
  знает смысла). «Минималки» = чистый markdown + граф в JSON, навигируемые CLI-агентом; вектор-
  стор/эмбеддинги — только если упрёмся.

**Следствие для приоритетов:** **контент раньше интерфейсов.** Сушка — это не гигиена ради
гигиены, а **фундамент под AI-слой** (грязная дока → бесполезный RAG). Это же снимает старый
страх «рефакторинг усыпляет»: у уборки появился внешний результат ниже по течению.

## 3. Арка (как всё связано)

```
сушка пакета + тестируемая дока (substrate)
        ↓
   чистый единый источник
        ↓
{RTD · сайт · AI-friendly render}  +  машиночитаемый код-граф
        ↓
   RAG-на-минималках для ИИ-CLI
```

## 4. Принятые решения / принципы (locked)

| Тема | Решение |
|---|---|
| Целевой язык | **Английский как единственный**, инкрементально (тронул файл — перевёл). Синхронную пару RU↔EN **не заводим** (двойная поддержка = ловушка). |
| Исходник доков | **В пакете** (doc-driven/парити). Наружу выносим сайт/рендеры, не исходник. |
| Косметика GitHub | `.gitattributes` `linguist-documentation` для `docs/**` + gitignore билдов — вместо сплита ради статистики. |
| Код-граф | Не первым шагом. Когда дойдём — тестируем твои внешние тулы/сервисы, а мои детерминированные методы (grimp/AST) = **эталон-бенчмарк** для их проверки. Артефакт графа — машиночитаемый (под RAG). |
| Метод работы | Мелкие шаги, полное понимание каждого, PR-на-батч, ничего необратимого без согласия. |

**Anti-todo (НЕ делаем):** формальный сплит на 3 репо; синхронная пара RU↔EN; переписывание
кэша/логов/nb/performance на внешние либы; вынос исходника доков; новые process-completion-report'ы.

## 5. Инвентаризация (итог; детали — в реестре)

Полный разбор — `package_drying_inventory_2026-07-20.md`. Крупные находки:
- `test_bquant_install/` **818M** untracked venv (не игнорится); `output/`+`outputs/` 59
  артефактов в git; `backup/indicators/` мёртвый код; `.vs/` IDE-мусор.
- `research/`: ⭐26 демо (00–04) · 🧪13 рисёрч в лабу (05–09) · 🔴13 скретч · 🚫39 артефактов.
- `devref/`: 27 keep · **98 в архив** · 2 снести (79% церемония).
- `CLAUDE=GEMINI=VIBE.md` идентичны → один источник.
- **Особое:** `gaps/zo/zodoctest/` (37 doc↔code валидаторов) осиротел → **поднять в `tests/`**
  как фундамент парити, не в архив.

## 6. Дорожная карта — мелкими шагами

Каждый шаг — маленький, самодостаточный, обсуждаемый и понимаемый ДО исполнения; отдельный
коммит/PR. Порядок — от самого безопасного.

### Батч A — мусор и артефакты (риск ~ноль, эффект ~860M)
- **A1.** `.gitignore`-хардненинг: `test_bquant_install/`, `.vs/`, `output/`, `outputs/`
  (только правила игнора, без удалений). Обсудить финальный список игнора.
- **A2.** `git rm --cached` для трекнутых артефактов (`output/`, `outputs/`, `.vs/`).
- **A3.** `rm -rf test_bquant_install/` (818M, untracked, вне git).
- **A4.** `git rm -r backup/indicators/` (мёртвый код; история в git).

### Батч B — архивация `devref/`
- **B1.** Создать `devref/archive/`, `git mv` 98 церемониальных файлов (§4 реестра).
- **B2.** `git rm` 2 устаревших (`zonan_v3_backup.md`, `zouni.md`).

### Батч C — сушка `research/`
- **C1.** Снести артефакты (`*_log.txt`, `outputs/`, ipynb-дубли).
- **C2.** Снести скретч/устар. (`test_*.py`, `bq.py`, `02_ind_lib.py`, `03_analysis_zones.py`).
- **C3.** MOVE-TO-LAB (05–09, methodology): док в `bquearch` + сверка наличия → снос из пакета
  после подтверждения.
- **C4.** Консолидировать демо `research/notebooks/00–04` ↔ `examples/` (один дом), переписать
  READMEs; при опустошении — упразднить `research/` в пакете.

### Батч D — фундамент doc-parity
- **D0.** ✅ Структурный аудит `docs/` (read-only) — карта, сироты, дубли, мисплейсменты.
  Результат: `docs_structure_audit_2026-07-22.md`. Вывод: парити-состав меняется после
  устаканивания структуры → сначала DOC-1..5, потом D1.
  - **DOC-1** навигация (подключить `zone_analysis_result.md` в toctree; placeholder-ссылки).
  - **DOC-2** research → лаба (**7 файлов**: 5 в `analytics/` + `analytical_philosophy.md`
    + `swing_analysis_results.md`; сверка в `bquearch` + `git rm` + починка ссылок).
    Handoff размещён: `bquearch/docs/bquant_docs_relocation_2026-07-22.md` (ждём вердикты).
  - **DOC-3** мисфайлы (`analytics/zones/swing.md` → `user_guide/`; судьба `analytics/`).
  - **DOC-4** дедуп `api/analysis/` (коллизии `pipeline.md`/`strategies.md`, единый источник сниппетов).
  - **DOC-5** мета/скрэтч (`BUILD_ISSUES.md` → архив; `_build/` → Батч A).
- **D1.** Оценить 37 `zodoctest/` валидаторов против *устоявшегося* API (что живо, что протухло).
- **D2.** Поднять рабочие в `tests/`, подключить к сьюту (живая проверка «примеры доков = код»).

### Батч E — гигиена
- **E1.** Свести `CLAUDE/GEMINI/VIBE.md` к одному источнику.
- **E2.** Починить G7 (3 красных теста loader) → полностью зелёный сьют.
- **E3.** `.gitattributes` linguist для `docs/**`.
- **E4.** Актуализировать `CLAUDE.md` под реальность.

## 7. Открытые решения (нужны от владельца)
1. `zodoctest/` (37) → в `tests/` (рекоменд.) или в архив?
2. `examples/` как канон + упразднить `research/` в пакете — ок?
3. Механика единого гайда (симлинк / генерация / просто `CLAUDE.md`)?
4. MOVE-TO-LAB (05–09) — сверить наличие в `bquearch` до сноса; кто двигает?
5. `.cursor/rules` + `.depr_rules` — оставить как editor-config или свести?

## 8. Лог прогресса
- 2026-07-20 — инвентаризация проведена (реестр создан); мастер-план согласован. Исполнение
  ещё не начато. Следующий шаг: обсудить и запустить **A1** (или закрыть §7).
- 2026-07-22 — **D0 выполнен** (структурный аудит `docs/`, read-only, ни одна дока не тронута).
  Артефакт: `docs_structure_audit_2026-07-22.md`. Найдено: 4 сироты, 5 research-файлов
  на вынос в лабу, кластер дублей `api/analysis/`, парити-красный флаг в `swing_strategies.md`,
  34M `_build/` в git. Хвост Батча D пересобран (DOC-1..5 → D1). Следующий шаг: обсудить
  порядок DOC-1..5 (рекоменд. начать с DOC-1 — риск~0, обратимо).

---

*Живой документ. Обновлять §7 (решения) и §8 (прогресс) по мере движения.*
