# zodoctest — триаж валидаторов (D1, 2026-07-24)

Прогон всех 36 валидаторов `venv_bquant/bin/python <file>` (exit 0 = pass).
Цель: понять, что живо / протухло / поймало реальный баг, перед промоушеном в `tests/` (D2).

## Итог: 26 → 28 зелёных / 10 → 8 падают

Исходный прогон: 26/36 зелёных. После доко-фиксов D1 (2 реальных бага) + правки одного
стейл-пути в самом валидаторе стало **28/36 зелёных**. Ниже таблица по исходным 10 падениям.

### Зелёные (26) — кандидаты в промоушен (D2)
Проходят против текущего API. Стиль — печать + `return bool` + `sys.exit`, для `tests/`
нужна конвертация в pytest-ассерты.

```
test_data_readme        test_developer_guide_readme  test_examples_regression_demo
test_examples_validation_demo  test_extension_guide   test_indicators_readme
test_loader             test_macd                    test_performance
test_processor          test_samples                 test_schemas
test_statistical        test_user_guide_quick_start  test_utils
test_validator          test_pipeline (12/12)        test_exceptions (4/4)
test_base (5/5)         test_index_rst (6/6)          test_logging (7/7)
test_nb (7/7)           test_config (8/8)             test_core_readme (8/8)
test_analysis_readme (9/9)  test_api_readme (9/9)
```

### Падения (10) — с диагнозом

| Валидатор | Причина | Класс | Действие |
|---|---|---|---|
| `test_user_guide_readme` | `../MIGRATION_v2.md` → реально `../migration/MIGRATION_v2.md` | **реальный баг доки** | ✅ ПОЧИНЕНО |
| `test_strategies` (6/7) | ссылка `devref/gaps/swing_detection_approaches.md` → уехала в `devref/archive/` | **реальный баг доки** | ✅ ПОЧИНЕНО |
| `test_zones` (11/12) | legacy `find_support_resistance`/`ZoneAnalyzer`: `.total_seconds()` на `int`-duration (код `__init__.py:442`) | deprecated-**код** (не дока), удаление в v3.0.0 | пропуск, зафиксировано |
| `test_docs_readme` (2/5) | нет `sphinx-build` в PATH; pip externally-managed; нет `docs/_static`, `docs/_templates` | **env** + мелкий config-gap | не промоутить как есть |
| `test_tutorials_readme` | `pip help install` возвращает non-zero | **env** | не промоутить как есть |
| `test_examples_strategies_demo` | `LIBRARY indicator 'zigzag' from 'pandas_ta' not found` | **env/dep** (pandas_ta без zigzag) | флаг: хрупкость zigzag-стратегии |
| `test_examples_readme` (crit=50) | то же + каскад по примерам | **env/dep** | требует разбора в D2 |
| `test_examples_universal_zones` | падение исполнения example-скрипта | смешанное | требует разбора в D2 |
| `test_examples_macd_zone_analysis` | traceback в выводе example-скрипта | смешанное | требует разбора в D2 |
| `test_visualization_readme` (crit=2) | `KeyError: 'macd_hist' not in columns` — пример визуализации без шага `.with_indicator('custom','macd')` | вероятно **стейл-пример** | разбор в D2 |

## Выводы для D2 (промоушен в tests/)

1. **«Поднять все 37» — премиса не выдержала.** Часть валидаторов **env-зависимы**
   (sphinx-build/pip) и непортируемы в CI; один тестирует **умирающий** legacy-код;
   несколько завязаны на **pandas_ta zigzag**, которого нет в текущем окружении.
2. **Промоутить стоит курируемое подмножество** ~20–24 зелёных (parity: импорты, сигнатуры,
   примеры), сконвертировав print+`return bool` → pytest-ассерты.
3. **Флаг (код, не дока):** ZigZag-свинг-стратегия падает без pandas_ta `zigzag` —
   реальные примеры её не могут выполнить (тесты сьюта зелены только на моке
   `tests/fixtures/swing_mocks.py`). Кандидат в отдельный robustness-тикет.
4. **Мелкий config-gap:** отсутствуют `docs/_static`, `docs/_templates` (Sphinx варнит).

*Первичные логи: скретч `ztriage/*.out` сессии. Артефакт создан в рамках D1.*
