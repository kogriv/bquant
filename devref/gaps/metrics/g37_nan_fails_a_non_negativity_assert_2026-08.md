# G37 — NaN проваливает проверку «≥ 0», и зона теряет все метрики волатильности

**Заведён:** 2026-08-31 при последовательном проходе по докам (волна 2).
**Статус:** ✅ **исправлен 2026-08-31.**

---

## 1. Симптом

При включённой стратегии `volatility='combined'` часть зон остаётся без
`volatility_metrics`. На встроенном сэмпле (MACD по линии, 32 зоны) — три зоны
длительностью **1, 2 и 20** баров. Двадцать — не опечатка: зоны в 3 бара метрики
получают, а зона в 20 баров нет.

В логе — трейсбек:

```
AssertionError: bollinger_width_std must be >= 0
  bquant/analysis/zones/strategies/volatility/combined.py:104  result.validate()
  bquant/analysis/zones/strategies/base.py:472
```

## 2. Причина

`bb_length = 20`. У зоны ровно в 20 баров скользящее окно наполняется **один раз**:

```
непустых значений ширины Боллинджера: 1  →  width.std() = nan
```

Стандартное отклонение по одному значению — `NaN`. Дальше:

```python
assert self.bollinger_width_std >= 0
```

`NaN >= 0` — `False`. Проверка, написанная против отрицательных значений, срабатывает
на «величина не определена», роняет весь набор метрик зоны и сообщает об этом
неверными словами: величина не отрицательна, её просто нет.

## 3. Три отдельные вещи, которые здесь не так

1. **NaN трактуется как нарушение инварианта.** Неопределённость — не то же самое, что
   недопустимое значение. Правильный ответ для зоны, где окно не наполнилось, —
   отсутствие поля с причиной, а не падение всей группы метрик.
2. **Отказ виден только в логе.** В результате зона просто не имеет
   `volatility_metrics`, и отличить «не посчиталось» от «стратегия не включалась»
   нельзя. Тот же силуэт, что G35.
3. **Инвариант проверяется `assert`.** Под `python -O` он исчезнет вместе с проверкой —
   и вместо потери метрик получится `NaN`, уехавший в статистику.

## 4. Починка

- В `VolatilityMetrics.validate()` (`strategies/base.py:472`) отличать `NaN` от
  отрицательного значения: `NaN` — законное «не определено».
- Для зоны короче окна — не считать ширину вовсе и сказать об этом, по образцу тестов
  гипотез: они возвращают `error` с причиной, а не правдоподобное число.
- Проверки инвариантов перевести с `assert` на явную ветку с исключением.

## 5. Радиус

| Где | Что |
|---|---|
| `bquant/analysis/zones/strategies/volatility/combined.py:104` | место падения |
| `bquant/analysis/zones/strategies/base.py` | **39 таких `assert`** в одном файле — все с той же уязвимостью к `NaN` и все исчезающие под `-O`. Свинги, форма, дивергенции, объём: у любой метрики, которой не хватило данных, тот же путь |
| `docs/user_guide/zone_analysis_result.md` | описание `volatility_metrics` — оговорка добавлена |


## 7. Как починено (2026-08-31)

**Отсутствие выражается `None`, а не нулём.** `bollinger_width_pct`,
`bollinger_width_std`, `bollinger_squeeze_ratio` и счётчики касаний стали
`Optional`. Разброс ширины считается только при двух и более наполненных окнах —
`std` по одному значению не «ноль разброса», а отсутствие величины.

**Композит отсутствует вместе со своими компонентами.** `volatility_score` на две
трети складывается из боллинджеровских слагаемых; без них число по шкале 0–10
несопоставимо с другими зонами, поэтому его нет, и вместе с ним нет подписи-режима.
Проверка пинит это в обе стороны: счёт без режима и режим без счёта одинаково
запрещены.

**Ветка `except` перестала выдумывать.** Она возвращала полный набор нулей со
`squeeze_ratio: 1.0` — правдоподобное измерение вместо признания, что посчитать не
удалось. На встроенном сэмпле так «измерялись» 13 зон из 32.

**39 `assert` заменены на `_require`,** поднимающий `AnalysisError`. У `assert` два
негодных здесь свойства: он исчезает под `python -O` вместе с проверкой и объявляет
ошибкой программиста то, что является свойством данных. Отдельная проверка обходит
AST модуля и краснеет на возвращении `assert`.

**Побочная находка при проверке.** Метрики терялись ещё в одном месте, и это место —
**сообщение о них**: строка отладочного лога форматировала `volatility_score:.2f`, на
`None` падала с `TypeError`, падение ловил окружающий `except`, и посчитанные метрики
выбрасывались. Замер после правки: группа есть у 30 зон из 32 (раньше — у 29, из
которых 13 несли выдуманные нули); без группы остались только зоны в 1 и 2 бара, где
стратегия честно отказывается по минимальной длине.

**Проверка:** `tests/unit/test_an_absent_metric_is_not_a_zero.py` — 6 утверждений, все
шесть краснеют на коде до правки.
