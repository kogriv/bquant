# CHANGE TRACE LOG — 2026-09-05

[G57 — контракты данных: десять находок аудита, один хелпер OHLC на три слоя]

[not_included] [Technical] Замер AQ-014–020, 024–026 до правки, каждая воспроизведена: пустой кадр с колонками — «валидно»; `high < low` в каждой строке — схема «валидно», валидатор «нет»; пустой кадр в `validate_data_completeness` — доли `NaN` (0/0); 6-колоночный MT CSV без заголовка — **19 строк**, колонки `['3336.94', '3344.77', …]`, индекс строки; колонка времени с 1 разобранным из 5 — `DatetimeIndex` с 4 `NaT`; preloaded: непорядок и пересечение принимаются, строки-даты отклоняются вопреки докстрингу, `'2h'` расширяет 31 → 35 баров, дубликаты оси не проверяются; зоны `[0..9],[5..14],[3..20]` — один сегмент соседей; постоянная цена — фильтр выбросов удаляет **все 50 строк**; `london_ny_overlap` — `object` с 40 `NaN`; `true_range` — `high-low` в одной функции и True Range в другой
[included] [Fixed] bquant/data/schemas.py — `ohlc_violations(df)`: три отношения OHLC один раз; `DataSchema.frame_rules`/`add_frame_rule`/`min_rows`; кадр без строк и обязательное поле без значений — отказ; `OHLCVSchema` добавляет три frame-правила
[included] [Fixed] bquant/data/validator.py — `_validate_ohlc_relationships` через хелпер (все три отношения); `validate_data_completeness` на пустом кадре — вердикт до цикла долей
[included] [Fixed] bquant/data/loader.py — сниффинг MT-формата по сырому семплу **до** `index_col`; `_is_mt_format_without_headers`: первая колонка разбирается как время в каждой строке, колонки 1–5 числовые (прежняя проверка `isinstance(str(v), str)` истинна для чего угодно); предупреждения загрузчика по всем трём отношениям OHLC
[included] [Fixed] bquant/data/processor.py — `resolve_time_index`: частичный разбор колонки времени — `ValueError` с числом и примерами; `remove_price_outliers`: одна маска по исходному кадру, `std == 0` — нет выбросов, `NaN` — не выброс; `detect_market_sessions`: `london_ny_overlap` булева без пропусков; `calculate_true_range()` — каноническое определение, `calculate_derived_indicators` пишет `intrabar_range` и настоящий `true_range`, `add_technical_features` зовёт ту же функцию
[included] [Fixed] bquant/analysis/zones/detection/preloaded.py — строки-даты разбираются (числа — позиции, отклоняются по имени); ось данных уникальна и монотонна; зоны сортируются, пересечение / `end < start` / повтор `zone_id` — `ValueError`; допуск снапит границу к ближайшему бару (`get_indexer(method='nearest', tolerance=…)`) и не расширяет зону; объявленные границы — в `indicator_context`
[included] [Fixed] bquant/analysis/zones/sequence_analysis.py — соседство `start == prev_end + 1`; `start <= prev_end` — `ValueError` по имени
[included] [Added] tests/unit/test_data_contracts_agree_between_layers.py — 22 проверки по всем десяти пунктам. Мутации: `min_rows` не проверяется — 1; `ohlc_violations` пуст — 2; сниффер после `index_col` — 2; частичный разбор принят — 1; допуск расширяет — 1; соседство `<=` — 1; старый цикл выбросов — 2; `true_range = high-low` — 1
[not_included] [Technical] Первая версия разбора дат в preloaded звала `pd.to_datetime` и на числах: `pd.to_datetime(10)` — 1970 год, и e2e-тест «позиции отклоняются по имени» упал. Числовые колонки не трогаются — их отклоняет `_require_comparable_clock`
[included] [Changed] docs/api/data/processor.md, docs/api/data/schemas.md, docs/api/data/loader.md, docs/tutorials/preloaded_zones_workflow.md — по прогону: одиннадцать производных колонок, `intrabar_range` и `true_range`; таблица схемы — пустой кадр, пустое обязательное поле, frame-правила; допуск в туториале — «снапит, не расширяет»: с `'2h'` те же 41/31/51 (было 44/35/54)
[included] [Added] devref/gaps/data/g57_data_contracts_disagreed_between_layers_2026-09.md
[not_included] [Technical] Сьют на G57, чистый клон, оба плеча: pandas 3.0.5 — **3338 passed, 33 skipped, 0 failed**; pandas 2.3.3 — **3338 passed, 33 skipped, 0 failed** (+28 к 3310: 22 новых, 6 добавленных проверок доков). Батарея 11 примеров и 24 рисёрч-скриптов — 35 из 35 с кодом 0
