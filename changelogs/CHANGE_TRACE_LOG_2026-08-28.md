# Трейслог изменений — 2026-08-28

[P2-A начат: CLI перестал выдавать не то, что обещает его имя]

[not_included] [Technical] Повод не из плана, а из замера. `bquant/cli.py` — это `[project.scripts] bquant = "bquant.cli:main"`, то есть исполняемый файл, появляющийся в `PATH` после `pip install bquant`, и он же описан в `README.md`, объявленном в `pyproject.toml` как `readme` — витрина PyPI. Тестов на модуль было **ноль**, поэтому полностью зелёный сьют (1540 passed) о его дефектах не знал
[included] [Added] devref/architecture/p2a_cli_backlog_2026-08.md — бэклог стадии P2-A. Роадмап (§3) прямо пишет, что CLI-AI тул «зреет из существующего слабого `bquant/cli.py`», так что починка CLI — не отвлечение от плана, а его первый шаг
[included] [Changed] devref/architecture/package_roadmap_2026-07.md — P2-A помечен «в работе», со ссылкой на бэклог и с указанием повода
[included] [Added] tests/unit/test_cli_says_what_it_does.py — 15 проверок, **написаны до правки**. Прогон против `274ab46`: 4 из 5 первых пинов красные, зелёный только `list` — команда, которая и не была сломана. Проверки чёрного ящика: гоняют `main()` так же, как терминал, и смотрят на произведённое
[included] [Fixed] bquant/cli.py — **дефект 1: `analyze` считал MACD и выбрасывал его.** Строка 44 клала результат в `result`, и переменная больше нигде не читалась; в график уходил сырой кадр. Замер до правки: одна трасса `Close`, вхождений «macd» без учёта регистра — ноль, при том что в лог печаталось `Calculating MACD (12, 26, 9)`. Рядом лежал невызванный `plot_macd_with_zones`, умеющий брать колонки по ролям. Замер после: 32 зоны, трассы `MACD`/`Signal`/`Histogram`, 32 размеченные зоны
[included] [Fixed] bquant/visualization/charts.py — **дефект 2: ось времени выдумывалась.** `_prepare_datetime_index` искал `timestamp`/`date` и **не знал про `time`** — имя из собственного стандарта проекта (AGENTS.md, «Column Standards»), — а не найдя, синтезировал ось `date_range('2024-01-01', periods=…, freq='1H')`. `get_sample_data()` отдаёт кадр именно с `time` и `RangeIndex`, поэтому срабатывала как раз ветка выдумывания. Замер: график начинался с `2024-01-01T00:00:00`, данные — с `2025-06-11 20:00+07:00`. Это хуже умолчания: график не молчал о времени, он **утверждал** даты, которых в данных нет, и подписывал ими реальные цены. Синтез убран; кадр без распознаваемого времени рисуется по позиционной оси с предупреждением
[included] [Fixed] bquant/visualization/charts.py — попутно: функция правила `data.index` на месте, то есть меняла кадр вызывающего как побочный эффект отрисовки. Теперь работает с копией
[included] [Fixed] bquant/{cli,visualization/charts}.py — **дефект 3, вскрытый уже своей же правкой.** `ZoneInfo.start_time`/`end_time` — это **позиция**, если анализ шёл по кадру с `RangeIndex`. Пока ось была выдумана, обе величины были одинаково бессмысленны и промах не читался; стоило поставить настоящую временную ось — plotly стал читать `x0=0` как эпоху, и все 32 зоны уехали в 1970 год за левый край. Форма прежняя: прямоугольники **есть**, их ровно столько, сколько зон, проверка «зоны размечены» проходит — а размечено не то место. Починено на двух уровнях: CLI ставит время на индекс до анализа (`_with_time_index`), поэтому времена настоящие и в самом результате, а не только на картинке; график (`_bound_on_axis`) переводит позиционную границу через индекс, а зону вне кадра **не рисует** — лучше не закрасить, чем закрасить наугад. Пин смотрит на координаты, а не на факт наличия
[included] [Fixed] bquant/visualization/charts.py — строгая проверка `is None` вместо прежней проверки на истинность: зона, начинающаяся в позиции `0`, раньше молча пропускалась, потому что `0` ложно
[included] [Added] bquant/visualization/charts.py — `plot_zones_over_indicator` + `create_zones_chart`. `plot_macd_with_zones` требует тройку ролей `line`/`signal`/`hist` и отказывается без неё, а RSI и AO объявляют **одну** роль `value` — то есть зоны по любому не-MACD осциллятору были непоказуемы, хотя пайплайн индикатор-агностичен. Новый путь рисует то, что в кадре есть; если не резолвится ни один осциллятор — отказывает, а не рисует похожее. Разметка зон вынесена в общий `_shade_zones`
[included] [Added] bquant/cli.py — `--indicator macd|rsi|ao`. Зашить MACD в смысл слова «анализ» значило бы вернуть ту же слипшуюся идентичность-со-смыслом, что разводили в G8/G20. Замер: macd 32 зоны (bull/bear), rsi 64 (oversold/neutral/overbought), ao 36
[included] [Added] bquant/cli.py — `--json` и `--no-chart`: структурный вывод с `schema_version`. Потребитель здесь программа, и без номера она не отличит смену формы от смены данных. Текст и JSON строятся **из одной сводки** (`summarize`), поэтому разойтись не могут; на это отдельный пин
[included] [Changed] bquant/cli.py — `sys.exit` убран из библиотечных функций и оставлен только в `main()`: импортировавший модуль получал завершение процесса вместо ловимой ошибки. Ошибки предметной области печатаются по иерархии `bquant.core.exceptions`; неожиданное исключение больше не сводится к строчке текста — трассировка сохраняется
[included] [Added] docs/user_guide/cli.md — страницы про CLI не было **вовсе**. Команды, флаги, схема JSON, словари типов зон по индикаторам, вызов тех же функций из кода, ограничения. В toctree `docs/index.rst` и в индексе `docs/user_guide/README.md`
[included] [Changed] README.md — раздел «Command Line» переписан: команда описана как анализ зон, добавлены `--indicator` и `--json`, ссылка на страницу CLI. Это витрина PyPI, поэтому раздел, обещавший анализ и показывавший график цен, уезжал на страницу пакета
[not_included] [Technical] `test_docs_parity.py` + `test_scripts_parity.py`: 597 passed, 33 skipped

==================== COMMIT DIVIDER ====================

[Хвост: тихий режим и фактический прогон примеров]

[included] [Fixed] bquant/cli.py — `--quiet` после переписывания почти не отличался от обычного вывода: гасил одну вводную строку и печатал полный отчёт. Флаг обещает «минимальный вывод», поэтому краткая форма сделана настоящей — одна строка `32 зон (bear: 16, bull: 16)`. Важно, что она **с результатом**: у прежней команды тихий режим печатал только путь к файлу, то есть отчитывался о том, что не упал, а не о том, что нашёл. Краткость сокращает подробности, а не обязанность назвать посчитанное
[included] [Added] tests/unit/test_cli_says_what_it_does.py — `test_quiet_is_short_but_not_silent_about_the_result`: ровно одна строка, и в ней есть число и слово «зон»
[not_included] [Technical] Сьют не ловит сломанные примеры — прогнаны **фактически**, под `MPLBACKEND=Agg`: все 11 `examples/*.py` зелёные, оба рисёрч-скрипта визуализации зелёные, `scripts/analysis/run_macd_analysis.py XAUUSD 1h --sample-data --include-charts` даёт 32 зоны — столько же, сколько CLI на том же наборе
[not_included] [Technical] Сьют после правки CLI: **1561 passed, 36 skipped** (было 1540/36). Прирост +21 разложен: 16 новых проверок CLI и +6 автосгенерированных парити-проверок на новую страницу доков (doc-parity 436 → 442); один из 16 добавлен этим коммитом

==================== COMMIT DIVIDER ====================

[G24: пакет писал в каталог собственной установки и падал там, где не мог]

[not_included] [Technical] Найдено проверкой того, что реально получает пользователь опубликованной 0.0.7, — то есть **установкой из PyPI**, а не прогоном по исходникам. По рабочей копии этот дефект не виден в принципе: `PROJECT_ROOT` там угадывает правильно
[included] [Added] devref/gaps/install/g24_package_writes_into_its_own_install_2026-08.md — разбор с замерами и планом
[included] [Changed] devref/gaps/gap_inventory_2026-07.md — заведена G24, реестр перестал быть пустым
[included] [Added] devref/architecture/release_0_0_8_backlog_2026-08.md — бэклог до релиза
[included] [Added] tests/unit/test_install_footprint.py — 10 проверок. Устроены так, чтобы **не зависеть от того, что мы в чекауте**: подставляют корень без признаков чекаута либо смотрят на импорт из отдельного процесса. Ключевой пин подменяет `pathlib.Path.mkdir` на бросающую заглушку и импортирует пакет — прямое утверждение «импорт не создаёт каталогов». Против HEAD: 9 из 9 красных
[included] [Fixed] bquant/core/config.py — `PROJECT_ROOT` («откуда читается код») разведён с новым `STATE_ROOT` («куда пакету можно писать»). `STATE_ROOT` = `$BQUANT_HOME`, иначе корень репозитория, если корень **похож на чекаут** (есть `pyproject.toml` рядом с пакетом), иначе `~/.local/share/bquant`. `DATA_DIR`/`RESULTS_DIR`/`PROCESSED_DATA_DIR` и путь лога висят на нём; `SCRIPTS_DIR`/`NOTEBOOKS_DIR` остались на `PROJECT_ROOT` — они только для чтения. Поведение разработчика не меняется
[included] [Fixed] bquant/core/config.py — убран цикл создания каталогов на импорте. Создание на импорте — не подготовка, а перенос отказа прав туда, где вызывающий ничего не может: он ещё не просил ничего записать, а импорт уже упал
[included] [Added] bquant/core/logging_config.py — `LazyDirRotatingFileHandler`: каталог заводится в момент первой записи, открытие файла отложено (`delay=True`). Иначе настройка логгирования (она случается на импорте) снова трогала бы файловую систему
[included] [Fixed] bquant/core/utils.py — модульный вызов `setup_project_logging` на импорте создавал каталог логов. Переведён на тот же отложенный обработчик
[included] [Changed] bquant/core/{logging_config,utils}.py — консольные логи **stdout → stderr**. Диагностика не данные: строка лога в stdout делает неразбираемым вывод любой команды, отдающей результат
[included] [Added] tests/unit/test_cli_says_what_it_does.py — `test_diagnostics_do_not_land_in_the_data_stream` + фикстура `json_run` переведена на **настоящий подпроцесс**. Прежняя проверка через `redirect_stdout` **пропускала живой дефект**: обработчики логгирования держат ссылку на исходный `sys.stdout`, поэтому подмена ловила только `print()`, а строки лога шли мимо неё в тот же поток, куда уходит JSON. Тест был зелёным, а `bquant analyze --json | jq` у пользователя падал. Вывод общий: проверяешь, что попадает в поток, — бери процесс, а не подмену потока внутри своего
[included] [Fixed] tests/integration/test_scripts_integration.py — два теста искали строку **лога** («analysis completed», «testing completed») в stdout, приняв диагностику за вывод программы. После переезда логов они покраснели, хотя скрипты отработали. Утверждают теперь то, что скрипт печатает (заголовок отчёта, число зон), а факт завершения ищут в stderr — то есть та же путаница потоков, что чинилась в коде, вычищена и из проверок
[not_included] [Technical] **Проверено единственным способом, каким это вообще проверяется**: собрано колесо, поставлено в чистое окружение, `chmod -R a-w site-packages`. Импорт проходит, `bquant list` даёт `exit=0`, в `site-packages` не появилось ни `logs/`, ни `results/`, ни `data/`. `STATE_ROOT` указывает в домашний каталог, `is_source_checkout(PROJECT_ROOT)` = False
[not_included] [Technical] Сьют: **1573 passed, 36 skipped**

==================== COMMIT DIVIDER ====================

[Публикация 0.0.8 и её проверка]

[not_included] [Technical] Опубликовано на PyPI: `bquant 0.0.8`, оба артефакта (whl 468 832 байта, sdist 1 043 578 байт), 2026-08-28T07:23 UTC. Номер был свободен — проверено до сборки (404 на версионном эндпоинте), версии на PyPI неизменяемы
[included] [Changed] CHANGELOG.md — раздел 0.0.8: два Fixed с замерами (отказ импорта при read-only каталоге установки; `analyze`, не создающий график и возвращающий 0), Added (`--indicator`, `--json`, `--no-chart`, `plot_zones_over_indicator`, `BQUANT_HOME`, страница CLI), таблица Changed (breaking) — каталоги, логи stdout→stderr, вывод команды, тип ошибок
[included] [Changed] pyproject.toml, bquant/__init__.py, uv.lock — версия 0.0.7 → 0.0.8 во всех трёх местах, `uv lock` в том же коммите. `docs/conf.py` привязан к `bquant.__version__` с 0.0.5, отдельного бампа не требует
[not_included] [Technical] Предполётная сверка колеса: 94 файла, `Version: 0.0.8`, `bquant/ml` отсутствует, `bquant/cli.py` на месте, `STATE_ROOT` в исходнике внутри колеса есть, цикла создания каталогов на импорте нет. `twine check` PASSED на обоих артефактах. Старые артефакты 0.0.7 удалены из `dist/` до сборки
[not_included] [Technical] **Проверено установкой из PyPI на машине без рабочей копии** (Python 3.12 против рабочей 3.13 — заодно проверка независимости от версии окружения). Импорт из `site-packages`, версия 0.0.8. В каталоге установки не появилось ни `logs/`, ни `results/`, ни `data/`; `STATE_ROOT` указывает в домашний каталог, `is_source_checkout(PROJECT_ROOT)` = False. При `chmod -R a-w site-packages`: импорт проходит, `bquant list` — `exit=0`, `bquant analyze --json` — `exit=0` и **чистый разбираемый JSON** на stdout (32 зоны, bull 16 / bear 16, канонические имена колонок). График: трассы `MACD`/`Signal`/`Histogram`, ось начинается с `2025-06-11T13:00:00`, размечено 32 зоны, первая — `x0=2025-06-11T13:00:00, x1=2025-06-11T15:00:00`. RSI: 64 зоны (overbought 14 / neutral 32 / oversold 18)
[not_included] [Technical] Грабли установочной проверки: первый прогон дал «No solution found … no version of bquant==0.0.8» через минуту после успешной загрузки — это **кэш индекса** (локальный кэш uv плюс CDN простого индекса), а не отсутствие пакета. Авторитетный ответ даёт версионный эндпоинт `/pypi/bquant/0.0.8/json`; установку гнать с `--refresh --no-cache`. Прошлый релиз ловил то же самое на агрегатном JSON
[not_included] [Technical] Документация: RTD отдаёт 0.0.8 и на `stable`, и на `latest`, страница `user_guide/cli.html` доступна на обеих (HTTP 200). Сборка идёт из GitLab, куда ушёл тот же пуш
[included] [Changed] devref/gaps/gap_inventory_2026-07.md — G24 перенесён в исправленные, раздел «Открыто» снова пуст
[included] [Changed] devref/architecture/release_0_0_8_backlog_2026-08.md — все шесть шагов закрыты

==================== COMMIT DIVIDER ====================

[P2-B начат: дока утверждала то, чего код не умеет (G25/G26)]

[not_included] [Technical] Найдено тем же приёмом, что G24 и мёртвый CLI, — **не читать, а исполнять**. Прогнаны все 213 python-блоков с импортом `bquant` из `docs/**/*.md` и `README.md`: 100 зелёных, 113 падают. Честная разбивка важнее числа: 84 падения — артефакт пробы (блоки-продолжения, опирающиеся на `data`/`df` из предыдущего блока), 5 — таймауты, содержательных 24, и часть из них — намеренные демонстрации отказов
[included] [Added] devref/gaps/docs/g25_docs_assert_what_the_code_cannot_do_2026-08.md, g26_vocabulary_lost_at_the_dict_boundary_2026-08.md — разборы с замерами
[included] [Added] devref/architecture/p2b_docs_as_product_backlog_2026-08.md; roadmap помечен «P2-B в работе»
[included] [Added] tests/integration/test_docs_examples_run.py — гардрейл: **150 самодостаточных примеров исполняются**, 21 с. Причина слепоты прежней проверки называется одной фразой: `test_docs_parity.py` проверяет, что **имена резолвятся**, но не что **вызов работает**, — поэтому неверный kwarg, несуществующий ключ словаря и пропущенный обязательный аргумент проходили насквозь
[not_included] [Technical] Самодостаточность блока определяется разбором `ast` **по модульной области видимости**: свободные имена на верхнем уровне = фрагмент, его не исполняем. Фрагменты сознательно **не сшиваются** по странице в один сценарий — сшивка ввела бы порядок, которого в доке нет. Лучше проверять меньше, но утверждать правду
[not_included] [Technical] Три собственные ошибки гардрейла, найденные и исправленные по ходу: (1) связывания собирались по всему дереву, поэтому аргумент `def analyze(self, data)` считался определением имени `data` — фрагмент объявлялся самодостаточным; (2) в тело функции не спускались вовсе, из-за чего терялись **декораторы**, которые вычисляются на модульном уровне (`@deprecated(...)` без импорта); (3) `SystemExit(0)` из `NotebookSimulator.finish()` считался отказом. Плюс изоляция: примерам даётся собственный `sys.argv` и пустой `stdin`, иначе они разбирали argv **pytest'а** и падали `SystemExit: 2` — диагноз про стенд, а не про пример
[not_included] [Technical] Ожидаемые падения объявлены явно (`EXPECTED_TO_FAIL`, 15 записей с причинами) — намеренные демонстрации отказов и примеры, ждущие файл читателя. Обратная проверка тоже стоит: пример из реестра, который **отработал**, роняет тест. Она сразу и сработала — поймала две мои неверные записи
[included] [Fixed] Тринадцать мест, где дока обещала то, чего код не умеет: `factory.md` (`info['parameters']` — ключей четыре, такого нет), `strategies.md` (`create_swing_strategy(legs=…)` — сигнатура `(config=None)`), `extension_guide.md` (`get_swing_strategy` возвращает **экземпляр**, а комментарий гласил «Get strategy class»; `stats['by_type']` — счётчики лежат в корне), `zone_detection_strategies.md` (звал несуществующие `.with_data(...)` и `pipeline.run()`), `developer_guide/README.md` (два демо-компонента не принимали `column_schema`/`vocabulary`, которыми их зовёт анализатор), `MIGRATION_v2.md` (`get_sample_data('btc_hourly')` — наборов два), `logging.md` (закрывающий fence стоял **после** абзаца, проза рендерилась как Python на сайте), `loader.md` (вывод логгера помечен как ```python), `quick_start.md` (обходил не тот уровень вложенности и предполагал `p_value` у каждого теста), `samples.md` (вызов без словаря типов зон), `AGENTS.md` (`get_data_path()` без обязательных аргументов — в файле инструкций, который читают все агенты)
[not_included] [Technical] **Поправка к собственному диагнозу.** G26 был заведён как «словарь типов зон теряется на границе, нужен новый параметр». Диагноз неверен: `vocabulary=` **уже есть** у `run_all_tests` и `run_all_hypothesis_tests` и **уже документирован** в `api/analysis/statistical.md`. Замер: без словаря 5 тестов из 7 дают `p_value`, со словарём 7 из 7. Дефект был только в двух страницах доки. Урок: прежде чем объявлять «возможности нет», проверить сигнатуру — отказ, который объясняет, чего ему не хватает, обычно и означает, что недостающее можно передать
[not_included] [Technical] Гардрейл проверен **дифференциально**: вернул `info['parameters']` в `factory.md` — тест покраснел; восстановил — зелёный
[included] [Fixed] tests/integration/test_docs_examples_run.py — **гардрейл протекал**, и это видно только полным прогоном. Примеры, регистрирующие свою стратегию (а без регистрации их и не покажешь), оставляли имя в глобальном реестре, и дальше по сьюту падали **два чужих теста** — `test_registry_lists_all_strategies` и `test_registry_list_all_info`. Проверка, которая портит состояние другим, хуже отсутствующей: она роняет чужой тест и посылает искать причину не там. Добавлена фикстура `isolated_registries` (слепок `ZoneDetectionRegistry` и `StrategyRegistry` до, восстановление после) и пин на саму изоляцию. Проверено в обоих порядках запуска

==================== COMMIT DIVIDER ====================

[P2-B этап 2: охват проверок доков (G27)]

[not_included] [Technical] Замер сразу после того, как встал гардрейл G25: под проверкой **147 блоков из 433**. Дыра не в строгости, а в охвате — три разные
[included] [Added] devref/gaps/docs/g27_doc_checks_are_limited_by_reach_2026-08.md; этап 2 в бэклоге P2-B; запись G27 в реестре гэпов
[not_included] [Technical] **Поправка к собственной оценке, сделанной до замера.** Я сказал владельцу, что снятие фильтра «поднимет поле зрения с 230 блоков до ~400». Неверно: из 171 блока без слова `bquant` самодостаточны только **20**, остальные и так фрагменты. Шаг 1 добавляет 20 блоков, а не 170; ценность в принципе, а не в числе
[included] [Fixed] tests/integration/test_docs_examples_run.py — убран отбор блоков **по подстроке** «bquant». Это не свойство блока, а совпадение символов: блок, где импорт стоял абзацем выше, отсеивался, хотя он про пакет. Той же формой промаха из проверок уже дважды выпадали целые файлы (`README.md`, `docs/index.rst`) — чинили каждый раз охват, а не проверку. Критерий теперь: блок **разбирается** и **самодостаточен**
[included] [Fixed] tests/integration/test_docs_examples_run.py — анализ свободных имён переписан **по областям видимости**. Обе прежние версии были неверны в разные стороны, и обе ошибки поймал прогон: обход всего дерева считал аргумент `def analyze(self, data)` определением имени `data`; только модульный уровень пропускал шаблон `def main(): … get_sample_data(…)` с вызовом `main()` внизу, хотя импорт остался в предыдущем блоке страницы
[included] [Added] tests/integration/test_docs_examples_run.py — `test_a_python_fence_contains_python`: блок, подписанный `python`, обязан им быть. Претензия отдельная от «пример не работает», и цена своя: на сайте такой блок подсвечен как исполняемый код, а для проверок **невидим** — разбор падает, блок молча выпадает из выборки
[included] [Changed] 32 блока в 6 файлах переподписаны ```python → ```text (19 из них — перечни сигнатур в `api/core/nb.md`, остальные — наброски структур и куски цепочек). Дифф ровно 32 строки, только подписи; код не тронут
[included] [Added] tests/unit/test_docs_call_signatures.py — третий слой: **вызов сверяется с реальной сигнатурой без исполнения**, 401 проверка. Нужен из-за арифметики: исполнимы 167 блоков из 433, а 85 используют пакет, опираясь на предыдущий блок. Сшивать их нельзя (порядок, которого дока не обещает), сверять — можно: страница сама объявляет своими импортами, что значит каждое имя. Неоднозначное имя (импорт из двух мест, переприсваивание) пропускается, а не угадывается
[included] [Fixed] docs/api/analysis/strategies.md — слой сверки сразу поймал два вызова `create_swing_strategy('find_peaks', prominence=…)` и `('pivot_points', left_bars=…)` при сигнатуре `(config=None)`. Оба **во фрагментах**, куда исполнение не доберётся никогда, — ровно то, ради чего слой заводился
[included] [Fixed] docs/api/analysis/{README,zones}.md — **`ZoneFeaturesAnalyzer` и `ZoneSequenceAnalyzer` числились «deprecated»**, при том что пайплайн собран именно на них и они вызываются из пакета 10 раз. Плюс четыре legacy-имени (`Zone`, `find_support_resistance`, `ZoneAnalyzer`, `extract_zone_features`) объявлялись устаревшими «с v2.1», хотя **в коде нет ни `DeprecationWarning`, ни маркера устаревания**. Объявлять устаревшим то, за чем ничего не стоит, значит обещать переходный период, которого нет; по конвенции проекта их и не заводят. Доки теперь описывают положение как есть
[included] [Fixed] README.md, docs/user_guide/quick_start.md, docs/examples/README.md — `examples/02_macd_zone_analysis.py` трижды подписан как «Migration Guide — переход с deprecated API». Файл называется «MACD Zone Analysis (Universal Pipeline)» и никакой миграции не содержит: ссылка резолвилась, врало описание — а описание проверкой ссылок не покрывается
[included] [Fixed] README.md — из витрины PyPI убраны два просроченных утверждения: «ML Readiness: модульная структура под будущий ML» (пакет `bquant/ml` удалён в 0.0.7, обе его публичные функции всегда бросали `NotImplementedError`) и «Test Suite: 1155 tests» (на деле 1725+). Счётчик заменён отсылкой к CHANGELOG, чтобы не протухал снова
[included] [Fixed] docs/README.md — страница-сирота (ни ссылки, ни записи в toctree) ссылалась на несуществующий `SETUP_READTHEDOCS.md`. Упоминание было в бэктиках, а не ссылкой, поэтому проверка ссылок его не видела
[not_included] [Technical] **Открытый вопрос владельцу, не решаю сам:** удалять ли четыре legacy-имени. `extract_zone_features` используется 6 раз в коде и в 10 тест-файлах. По конвенции проекта переходных периодов не заводят, значит выбор бинарный — оставить как есть или вырезать целиком вместе с вызывающими. Это решение об API, а не починка доки

==================== COMMIT DIVIDER ====================

[G28: развели две «зоны» — они означали разное, а имена этого не говорили]

[not_included] [Technical] Найдено не замером, а спором. Владелец спросил, не надо ли «в духе DRY» вырезать legacy-имена. При разборе выяснилось, что вырезать нечего: `ZoneInfo` — **участок времени**, на котором осциллятор в одном состоянии (границы в барах, зоны идут встык, мощение), `Zone` — **полоса цены** (границы в цене, полосы перекрываются, покрывают не всю историю). Общего только слово
[not_included] [Technical] **Цена прежних имён уже была уплачена четырежды:** дока написала «`Zone` → `ZoneInfo`, deprecated» как будто есть путь миграции; я предложил «резать», приняв разное за старое и новое; владелец задал вопрос в той же рамке, потому что имена её и задают; возник ложный вопрос «как уложить S/R в мощение» — у него нет ответа, потому что он бессмыслен. Три часа разбора выросли из одного слова на два понятия
[included] [Added] devref/gaps/zone_types/g28_one_word_two_concepts_2026-08.md, devref/architecture/g28_zone_naming_backlog_2026-08.md; запись G28 в реестре
[included] [Added] tests/unit/test_two_kinds_of_zone.py — 4 пина. Утверждают **не факт переименования** (это проверялось бы однажды и ничего не защищало), а **различие моделей**: у одной есть `start_idx`/`end_idx` и нет ценовых границ, у другой наоборот; полосы цены не обзаводятся индексом бара. Против HEAD все четыре красные
[included] [Changed] bquant/analysis/zones/__init__.py — `Zone` → `PriceLevelZone`, `ZoneAnalyzer` → `PriceLevelAnalyzer`. Докстринги теперь объясняют **предмет**, а не ранг, и прямо говорят, что приставка `Universal` у второго анализатора означает независимость от индикатора, а не превосходство. Алиасов нет — как в G22
[not_included] [Technical] Регулярка по границам слова задела **прозу**: `logger.warning(f"Zone models not available")` стало `PriceLevelZone models not available`, и таких мест 11. Возвращено вручную. Урок прежний: `\bZone\b` не отличает имя класса от английского слова в сообщении
[included] [Removed] bquant/analysis/zones/zone_features.py — модульная `extract_zone_features`: чистый дубль метода `ZoneFeaturesAnalyzer.extract_zone_features`, звалась только из трёх тестов. **Метод не тронут — на нём стоит пайплайн.** Разделил их `codemap impact`: граф показал цепочку метод → `extract_all_zones_features` → `UniversalZoneAnalyzer.analyze_zones`, тогда как модульная функция из ядра не зовётся вовсе. Мой греп по имени давал «6 использований в пакете», склеивая метод с обёрткой — по нему я бы либо побоялся трогать, либо срезал живое
[included] [Changed] tests/unit/{test_analysis_structure,test_zones_analysis}.py — переведены на новые имена
[included] [Changed] docs/api/analysis/{zones,README}.md — раздел «Legacy API» переписан в «Вторая ветка: уровни цены» с таблицей различий. Вчера я привёл его к «legacy, но работает» — это было полдела: честно, но всё ещё в рамке «старое против нового»
[not_included] [Technical] **Найдено попутно, не чинилось:** написанный для доки пример упал — `PriceLevelAnalyzer` считает длительность как разницу дат, а `get_sample_data()` отдаёт позиционный индекс. Тот же корень, что у отложенного `ZoneInfo.start_time`: поле называется временем, а содержит то время, то позицию. Пример приведён к рабочему виду, анализатор не тронут — решается вместе с общим вопросом
[not_included] [Technical] **Внешний потребитель не затронут** — проверено прямым замером по его чекауту: голого `Zone` в коде ноль, `ZoneAnalyzer` пять раз и только прозой в брейнсторм-документе про его собственный класс. Из bquant он берёт исключительно пайплайн. Отдельно передана задача на его стороне (обновление, каталоги, роли вместо угадывания колонок) — реестр в /data/infra
[not_included] [Technical] **Codemap звался первым, до grep** — по операционному правилу, которое я в этой же сессии сперва нарушил. Граф дал направление (разделил метод и обёртку, показал, что остров замкнут), grep вынес вердикт по радиусу. Граф был протухшим на 4.8 суток — пересобран
