# Трейслог изменений — 2026-08-28

[P2-A начат: CLI перестал выдавать не то, что обещает его имя]

[not_included] [Technical] Повод не из плана, а из замера. `bquant/cli.py` — это `[project.scripts] bquant = "bquant.cli:main"`, то есть исполняемый файл, появляющийся в `PATH` после `pip install bquant`, и он же описан в `README.md`, объявленном в `pyproject.toml` как `readme` — витрина PyPI. Тестов на модуль было **ноль**, поэтому полностью зелёный сьют (1540 passed) о его дефектах не знал
[included] [Added] devref/architecture/p2a_cli_backlog_2026-08.md — бэклог стадии P2-A. Роадмап (§3) прямо пишет, что CLI-AI тул «зреет из существующего слабого `bquant/cli.py`», так что починка CLI — не отвлечение от плана, а его первый шаг
[included] [Changed] devref/architecture/package_roadmap_2026-07.md — P2-A помечен «в работе», со ссылкой на бэклог и с указанием повода
[included] [Added] tests/unit/test_cli_says_what_it_does.py — 15 проверок, **написаны до правки**. Прогон против `274ab46`: 4 из 5 первых пинов красные, зелёный только `list` — команда, которая и не была сломана. Проверки чёрного ящика: гоняют `main()` так же, как терминал, и смотрят на произведённое
[included] [Fixed] bquant/cli.py — **дефект 1: `analyze` считал MACD и выбрасывал его.** Строка 44 клала результат в `result`, и переменная больше нигде не читалась; в график уходил сырой кадр. Замер до правки: одна трасса `Close`, вхождений «macd» без учёта регистра — ноль, при том что в лог печаталось `Calculating MACD (12, 26, 9)`. Рядом лежал невызванный `plot_macd_with_zones`, умеющий брать колонки по ролям. Замер после: 32 зоны, трассы `MACD`/`Signal`/`Histogram`, 32 размеченные зоны
[included] [Fixed] bquant/visualization/charts.py — **дефект 2: ось времени выдумывалась.** `_prepare_datetime_index` искал `timestamp`/`date` и **не знал про `time`** — имя из собственного стандарта проекта (AGENTS.md, «Column Standards»), — а не найдя, синтезировал ось `date_range('2024-01-01', periods=…, freq='1H')`. `get_sample_data()` отдаёт кадр именно с `time` и `RangeIndex`, поэтому срабатывала как раз ветка выдумывания. Замер: график начинался с `2024-01-01T00:00:00`, данные — с `2025-06-11 20:00+07:00`. Это хуже умолчания: график не молчал о времени, он **утверждал** даты, которых в данных нет, и подписывал ими реальные цены. Синтез убран; кадр без распознаваемого времени рисуется по позиционной оси с предупреждением
[included] [Fixed] bquant/visualization/charts.py — попутно: функция правила `data.index` на месте, то есть меняла кадр вызывающего как побочный эффект отрисовки. Теперь работает с копией
[included] [Fixed] bquant/{cli,visualization/charts}.py — **дефект 3, вскрытый уже своей же правкой.** `ZoneInfo.start_time`/`end_time` — это **позиция**, если анализ шёл по кадру с `RangeIndex`. Пока ось была выдумана, обе величины были одинаково бессмысленны и промах не читался; стоило поставить настоящую временную ось — plotly стал читать `x0=0` как эпоху, и все 32 зоны уехали в 1970 год за левый край. Форма прежняя: прямоугольники **есть**, их ровно столько, сколько зон, проверка «зоны размечены» проходит — а размечено не то место. Починено на двух уровнях: CLI ставит время на индекс до анализа (`_with_time_index`), поэтому времена настоящие и в самом результате, а не только на картинке; график (`_bound_on_axis`) переводит позиционную границу через индекс, а зону вне кадра **не рисует** — лучше не закрасить, чем закрасить наугад. Пин смотрит на координаты, а не на факт наличия
[included] [Fixed] bquant/visualization/charts.py — строгая проверка `is None` вместо прежней проверки на истинность: зона, начинающаяся в позиции `0`, раньше молча пропускалась, потому что `0` ложно
[included] [Added] bquant/visualization/charts.py — `plot_zones_over_indicator` + `create_zones_chart`. `plot_macd_with_zones` требует тройку ролей `line`/`signal`/`hist` и отказывается без неё, а RSI и AO объявляют **одну** роль `value` — то есть зоны по любому не-MACD осциллятору были непоказуемы, хотя пайплайн индикатор-агностичен. Новый путь рисует то, что в кадре есть; если не резолвится ни один осциллятор — отказывает, а не рисует похожее. Разметка зон вынесена в общий `_shade_zones`
[included] [Added] bquant/cli.py — `--indicator macd|rsi|ao`. Зашить MACD в смысл слова «анализ» значило бы вернуть ту же слипшуюся идентичность-со-смыслом, что разводили в G8/G20. Замер: macd 32 зоны (bull/bear), rsi 64 (oversold/neutral/overbought), ao 36
[included] [Added] bquant/cli.py — `--json` и `--no-chart`: структурный вывод с `schema_version`. Потребитель здесь программа, и без номера она не отличит смену формы от смены данных. Текст и JSON строятся **из одной сводки** (`summarize`), поэтому разойтись не могут; на это отдельный пин
[included] [Changed] bquant/cli.py — `sys.exit` убран из библиотечных функций и оставлен только в `main()`: импортировавший модуль получал завершение процесса вместо ловимой ошибки. Ошибки предметной области печатаются по иерархии `bquant.core.exceptions`; неожиданное исключение больше не сводится к строчке текста — трассировка сохраняется
[included] [Added] docs/user_guide/cli.md — страницы про CLI не было **вовсе**. Команды, флаги, схема JSON, словари типов зон по индикаторам, вызов тех же функций из кода, ограничения. В toctree `docs/index.rst` и в индексе `docs/user_guide/README.md`
[included] [Changed] README.md — раздел «Command Line» переписан: команда описана как анализ зон, добавлены `--indicator` и `--json`, ссылка на страницу CLI. Это витрина PyPI, поэтому раздел, обещавший анализ и показывавший график цен, уезжал на страницу пакета
[not_included] [Technical] `test_docs_parity.py` + `test_scripts_parity.py`: 597 passed, 33 skipped

==================== COMMIT DIVIDER ====================

[Хвост: тихий режим и фактический прогон примеров]

[included] [Fixed] bquant/cli.py — `--quiet` после переписывания почти не отличался от обычного вывода: гасил одну вводную строку и печатал полный отчёт. Флаг обещает «минимальный вывод», поэтому краткая форма сделана настоящей — одна строка `32 зон (bear: 16, bull: 16)`. Важно, что она **с результатом**: у прежней команды тихий режим печатал только путь к файлу, то есть отчитывался о том, что не упал, а не о том, что нашёл. Краткость сокращает подробности, а не обязанность назвать посчитанное
[included] [Added] tests/unit/test_cli_says_what_it_does.py — `test_quiet_is_short_but_not_silent_about_the_result`: ровно одна строка, и в ней есть число и слово «зон»
[not_included] [Technical] Сьют не ловит сломанные примеры — прогнаны **фактически**, под `MPLBACKEND=Agg`: все 11 `examples/*.py` зелёные, оба рисёрч-скрипта визуализации зелёные, `scripts/analysis/run_macd_analysis.py XAUUSD 1h --sample-data --include-charts` даёт 32 зоны — столько же, сколько CLI на том же наборе
[not_included] [Technical] Сьют после правки CLI: **1561 passed, 36 skipped** (было 1540/36). Прирост +21 разложен: 16 новых проверок CLI и +6 автосгенерированных парити-проверок на новую страницу доков (doc-parity 436 → 442); один из 16 добавлен этим коммитом

==================== COMMIT DIVIDER ====================

[G24: пакет писал в каталог собственной установки и падал там, где не мог]

[not_included] [Technical] Найдено проверкой того, что реально получает пользователь опубликованной 0.0.7, — то есть **установкой из PyPI**, а не прогоном по исходникам. По рабочей копии этот дефект не виден в принципе: `PROJECT_ROOT` там угадывает правильно
[included] [Added] devref/gaps/install/g24_package_writes_into_its_own_install_2026-08.md — разбор с замерами и планом
[included] [Changed] devref/gaps/gap_inventory_2026-07.md — заведена G24, реестр перестал быть пустым
[included] [Added] devref/architecture/release_0_0_8_backlog_2026-08.md — бэклог до релиза
[included] [Added] tests/unit/test_install_footprint.py — 10 проверок. Устроены так, чтобы **не зависеть от того, что мы в чекауте**: подставляют корень без признаков чекаута либо смотрят на импорт из отдельного процесса. Ключевой пин подменяет `pathlib.Path.mkdir` на бросающую заглушку и импортирует пакет — прямое утверждение «импорт не создаёт каталогов». Против HEAD: 9 из 9 красных
[included] [Fixed] bquant/core/config.py — `PROJECT_ROOT` («откуда читается код») разведён с новым `STATE_ROOT` («куда пакету можно писать»). `STATE_ROOT` = `$BQUANT_HOME`, иначе корень репозитория, если корень **похож на чекаут** (есть `pyproject.toml` рядом с пакетом), иначе `~/.local/share/bquant`. `DATA_DIR`/`RESULTS_DIR`/`PROCESSED_DATA_DIR` и путь лога висят на нём; `SCRIPTS_DIR`/`NOTEBOOKS_DIR` остались на `PROJECT_ROOT` — они только для чтения. Поведение разработчика не меняется
[included] [Fixed] bquant/core/config.py — убран цикл создания каталогов на импорте. Создание на импорте — не подготовка, а перенос отказа прав туда, где вызывающий ничего не может: он ещё не просил ничего записать, а импорт уже упал
[included] [Added] bquant/core/logging_config.py — `LazyDirRotatingFileHandler`: каталог заводится в момент первой записи, открытие файла отложено (`delay=True`). Иначе настройка логгирования (она случается на импорте) снова трогала бы файловую систему
[included] [Fixed] bquant/core/utils.py — модульный вызов `setup_project_logging` на импорте создавал каталог логов. Переведён на тот же отложенный обработчик
[included] [Changed] bquant/core/{logging_config,utils}.py — консольные логи **stdout → stderr**. Диагностика не данные: строка лога в stdout делает неразбираемым вывод любой команды, отдающей результат
[included] [Added] tests/unit/test_cli_says_what_it_does.py — `test_diagnostics_do_not_land_in_the_data_stream` + фикстура `json_run` переведена на **настоящий подпроцесс**. Прежняя проверка через `redirect_stdout` **пропускала живой дефект**: обработчики логгирования держат ссылку на исходный `sys.stdout`, поэтому подмена ловила только `print()`, а строки лога шли мимо неё в тот же поток, куда уходит JSON. Тест был зелёным, а `bquant analyze --json | jq` у пользователя падал. Вывод общий: проверяешь, что попадает в поток, — бери процесс, а не подмену потока внутри своего
[included] [Fixed] tests/integration/test_scripts_integration.py — два теста искали строку **лога** («analysis completed», «testing completed») в stdout, приняв диагностику за вывод программы. После переезда логов они покраснели, хотя скрипты отработали. Утверждают теперь то, что скрипт печатает (заголовок отчёта, число зон), а факт завершения ищут в stderr — то есть та же путаница потоков, что чинилась в коде, вычищена и из проверок
[not_included] [Technical] **Проверено единственным способом, каким это вообще проверяется**: собрано колесо, поставлено в чистое окружение, `chmod -R a-w site-packages`. Импорт проходит, `bquant list` даёт `exit=0`, в `site-packages` не появилось ни `logs/`, ни `results/`, ни `data/`. `STATE_ROOT` указывает в домашний каталог, `is_source_checkout(PROJECT_ROOT)` = False
[not_included] [Technical] Сьют: **1573 passed, 36 skipped**

==================== COMMIT DIVIDER ====================

[Публикация 0.0.8 и её проверка]

[not_included] [Technical] Опубликовано на PyPI: `bquant 0.0.8`, оба артефакта (whl 468 832 байта, sdist 1 043 578 байт), 2026-08-28T07:23 UTC. Номер был свободен — проверено до сборки (404 на версионном эндпоинте), версии на PyPI неизменяемы
[included] [Changed] CHANGELOG.md — раздел 0.0.8: два Fixed с замерами (отказ импорта при read-only каталоге установки; `analyze`, не создающий график и возвращающий 0), Added (`--indicator`, `--json`, `--no-chart`, `plot_zones_over_indicator`, `BQUANT_HOME`, страница CLI), таблица Changed (breaking) — каталоги, логи stdout→stderr, вывод команды, тип ошибок
[included] [Changed] pyproject.toml, bquant/__init__.py, uv.lock — версия 0.0.7 → 0.0.8 во всех трёх местах, `uv lock` в том же коммите. `docs/conf.py` привязан к `bquant.__version__` с 0.0.5, отдельного бампа не требует
[not_included] [Technical] Предполётная сверка колеса: 94 файла, `Version: 0.0.8`, `bquant/ml` отсутствует, `bquant/cli.py` на месте, `STATE_ROOT` в исходнике внутри колеса есть, цикла создания каталогов на импорте нет. `twine check` PASSED на обоих артефактах. Старые артефакты 0.0.7 удалены из `dist/` до сборки
[not_included] [Technical] **Проверено установкой из PyPI на машине без рабочей копии** (Python 3.12 против рабочей 3.13 — заодно проверка независимости от версии окружения). Импорт из `site-packages`, версия 0.0.8. В каталоге установки не появилось ни `logs/`, ни `results/`, ни `data/`; `STATE_ROOT` указывает в домашний каталог, `is_source_checkout(PROJECT_ROOT)` = False. При `chmod -R a-w site-packages`: импорт проходит, `bquant list` — `exit=0`, `bquant analyze --json` — `exit=0` и **чистый разбираемый JSON** на stdout (32 зоны, bull 16 / bear 16, канонические имена колонок). График: трассы `MACD`/`Signal`/`Histogram`, ось начинается с `2025-06-11T13:00:00`, размечено 32 зоны, первая — `x0=2025-06-11T13:00:00, x1=2025-06-11T15:00:00`. RSI: 64 зоны (overbought 14 / neutral 32 / oversold 18)
[not_included] [Technical] Грабли установочной проверки: первый прогон дал «No solution found … no version of bquant==0.0.8» через минуту после успешной загрузки — это **кэш индекса** (локальный кэш uv плюс CDN простого индекса), а не отсутствие пакета. Авторитетный ответ даёт версионный эндпоинт `/pypi/bquant/0.0.8/json`; установку гнать с `--refresh --no-cache`. Прошлый релиз ловил то же самое на агрегатном JSON
[not_included] [Technical] Документация: RTD отдаёт 0.0.8 и на `stable`, и на `latest`, страница `user_guide/cli.html` доступна на обеих (HTTP 200). Сборка идёт из GitLab, куда ушёл тот же пуш
[included] [Changed] devref/gaps/gap_inventory_2026-07.md — G24 перенесён в исправленные, раздел «Открыто» снова пуст
[included] [Changed] devref/architecture/release_0_0_8_backlog_2026-08.md — все шесть шагов закрыты
