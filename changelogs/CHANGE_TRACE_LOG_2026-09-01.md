# Трейслог изменений — 2026-09-01

[Сверка синка с потребителем перед продолжением прохода по докам]

[not_included] [Technical] Сверка состояния обмена с приватной лабой: шесть issue за всё время, наших пять. Открыты `#1` (G12, **ни одной заметки за десять дней**), `#2` и `#3` (G10a/G10b, ответа нет, 31.08 дописан контекст про свинги), `#6` (условие выпуска 0.0.10 — все четыре пункта закрыты замером, держится открытым намеренно, до пинга о релизе). Незакрытых вопросов к нам нет ни одного
[not_included] [Technical] **Расхождение, найденное сверкой: правка, о которой я отчитался «исправлено у источника», не была опубликована.** 31.08 потребителю сообщено, что отозванный вывод исправлен в `docs/analytics/zones/swing_strategy_comparison_case_study.md` — в отчёте, из которого этот вывод к нему и пришёл. В рабочем дереве он был исправлен; в опубликованном — нет: **пятнадцать коммитов не отправлены ни на один из двух удалённых**, `origin/main` стоял на `85bfc2d`. Свою сторону потребитель поправил в тот же день, наша лежала локально. Сайт документации собирается с зеркала, значит отозванный вывод оставался читаемым ровно там, где его и прочли
[not_included] [Technical] Форма ошибки — та же, что разбиралась весь предыдущий день: сказанное было правдой буквально («исправлено») и неправдой по существу («у источника»). Правильная форма ответа, из-за которой никто не идёт проверять
[included] [Changed] Пятнадцать коммитов (`85bfc2d..d1e161f`) отправлены на оба удалённых. Греп диффа по правилам публичного репозитория чист: все совпадения — публичный адрес репозитория, публичная подпись автора и текст самого правила в `AGENTS.md`. Публикация поправки подтверждена прогоном по обоим зеркалам, а не фактом успешного push
[not_included] [Technical] Второе расхождение, к сведению и без правки: приёмка потребителя сделана против `eb4f720`, после неё в код пакета легли `aaa7583` (переименование пресета — **одобрено им заранее и явно**, но не прогнано) и `dcb004d` (G39 — **не объявлялось вовсе**). По грепу его чекаута G39 его не касается: `bquant.analysis.validation` не импортируется нигде, `walk_forward` у него собственный (`research/shared/zone_ts_models.py`). Но греп — предварительная фильтрация, а не проверка; решено добавить G39 строкой в пинг о релизе, тогда прогон на пине покроет обе правки числами, а не моим грепом

[Волна 3, блок данных: три дефекта под шестью страницами]

[not_included] [Technical] Шесть страниц справочника данных: `README`, `samples`, `loader`, `processor`, `schemas`, `validator`. Две из них (`processor`, `validator`) не переписывались с октября 2025 — то есть застали и переименование `IndicatorConfig`/`IndicatorSpec`, и удаление `MACDZoneAnalyzer`. Правило прохода прежнее: числа получать прогоном. Из шести страниц **три оказались с неисполнимыми примерами, а под тремя лежали дефекты кода**
[included] [Fixed] bquant/data/samples/{utils,datasets}.py — **G40: датасет объявлял один период, нёс другой, валидация отвечала «исправен».** `mt_xauusd_m15` объявлял `2025-05-20 … 2025-05-30` при данных за `2025-08-07 … 2025-08-22` — три месяца. `validate_dataset()` возвращала `is_valid: True`, потому что сверяла число строк и набор колонок, а период не проверяла вовсе. Это не «проверка пропустила ошибку» — проверки не было, а вердикт выдавался общий. Добавлена сверка объявленного с первой и последней записью данных; необъявленный период — тоже ошибка; совпадение видно в `stats['period']`, чтобы «проверено» отличалось от «пропущено» и в успешном случае. Реестр исправлен по замеру
[not_included] [Technical] Почему не поймал сьют: все проверки sample-данных работают на `tv_xauusd_1h` — единственном датасете, у которого метаданные совпадали. `mt_xauusd_m15` фигурировал ровно в одной строке, и она о содержимом не спрашивала. Тот же силуэт, что в G39: сьют проверял тип, с которым проблем нет
[not_included] [Technical] Остаток G40 оставлен открытым и записан: сгенерированный `embedded/mt_xauusd_m15.py` несёт `period_start=None` и список колонок, в который попала первая строка данных **от 2021 года** — MT-CSV идёт без заголовка. В одном сгенерированном файле два артефакта об одном предмете, и они не сходятся; третий — реестр, написанный руками. API читает реестр, поэтому испорченное просто лежит и выглядит достоверным. Чинить нечем: причина в извлечении, а проверить исправление можно только на исходном CSV, которого в репозитории нет и не должно быть
[included] [Fixed] bquant/data/processor.py — **G41: подготовка данных возвращала пустой кадр как успех.** Документированная пара `clean_ohlcv_data()` → `prepare_data_for_analysis()` на встроенном сэмпле давала `(0, 36)`: тридцать шесть колонок и ни одной строки, без исключения, с записью уровня INFO «Removed 1000 rows with missing values». Причина — `dropna()` по всему кадру при четырёх колонках маркеров дивергенций TradingView, пустых целиком. Намерение было записано в комментарии верно («отсев прогрева окон»); разошлись намерение и область применения — **величина посчитана не на том множестве, к которому применяется**, пятый случай этой формы за неделю
[included] [Changed] bquant/data/processor.py — отсев идёт по подмножеству «цель + признаки»; колонка, пустая во всех строках, признаком не считается и строк не отсеивает, её имя уходит в WARNING; пустой результат не возвращается, а поднимает `DataProcessingError` с числом исходных строк. На сэмпле теперь `(951, 36)` — снято ровно 49 строк прогрева `price_ma_50`, результат начинается с индекса 49
[included] [Fixed] bquant/data/schemas.py — **G42: схемная валидация подтверждала что угодно.** `DataSchema.validate_dataframe()` безусловно возвращала `is_valid=True` — на любом кадре, включая пустой и не содержащий ни одного объявленного поля; `validate_with_schema(sample, 'macd')` давала «валидно» при отсутствии всех трёх требуемых колонок. Признание «не реализовано» в результате **было** — в `recommendations`, то есть в поле, которое не читают, когда вердикт уже получен. Худшее место для такого признания
[included] [Changed] bquant/data/schemas.py — заглушка заменена реализацией по тем данным, которые схема и так несла: обязательные поля, типы, правила значений со счётчиком нарушений по колонкам, опциональные поля отдельно. Вариант «оставить заглушку, но заставить её отказываться (`NotImplementedError`)» рассматривался и отклонён: всё необходимое в схеме уже лежало, и отказ вместо тридцати строк был бы выбором в пользу меньшей работы, а не в пользу правды
[included] [Fixed] bquant/data/schemas.py — побочно: `IndicatorSchema('любое-имя-кроме-трёх-известных')` падал с `AttributeError: no attribute 'logger'`. Ветка «индикатор неизвестен — оставляем схему без ограничений» была написана аккуратно, с объясняющим комментарием, и ни разу не исполнялась
[not_included] [Technical] `research/notebooks/01_data_schemas.py` **демонстрировал** ложный вердикт: шаг «Валидация проблемных OHLCV данных» печатал `Валидны: True`. Скрипт показывал дефект как возможность. Правок не потребовал — он печатает то, что получил, и теперь получает правду: те же шаги дают `❌ Невалидны`
[included] [Added] tests/unit/test_the_declared_period_is_the_period_shipped.py (7 проверок, параметризованы по **всем** датасетам реестра), tests/unit/test_preparation_does_not_return_an_empty_frame.py (13), tests/unit/test_schema_validation_forms_a_verdict.py (9). Мутация: на дореформенном коде краснеют 4/7, 13/13 и 8/9 соответственно
[included] [Changed] docs/api/data/README.md — переписана. Два раздела «быстрого поиска» на 60 строк заменены таблицей модулей и шестью исполняемыми примерами. Оба перечня объявляли `load_all_data_files()` загружающей «без рекурсии» — параметров `pattern` и `recursive`, вокруг которых строилась оговорка, у функции нет
[included] [Fixed] docs/api/data/loader.md — **четыре объявленные сигнатуры из семи не совпадали с кодом.** `load_symbol_data` не имеет параметра `validate_data`; `load_xauusd_data` не имеет `data_source` и `quote_provider`; `load_all_data_files` не имеет `pattern` и `recursive`; `get_available_timeframes` объявлена как `(data_dir=None, data_source='tradingview')`, а требует **обязательный `symbol`**. Слой сигнатур этого не ловит: он проверяет вызовы в примерах, а здесь сигнатуры стояли прозой в перечне
[included] [Changed] docs/api/data/processor.md — переписана (136 → ~700 слов). Все примеры начинались с `df`, которого нигде нет. Перечень функций заканчивался оговоркой «если присутствуют в версии модуля» — присутствуют все пять; `resolve_time_index` не был назван вовсе, хотя без него три функции из десяти отказывают на sample-данных
[included] [Changed] docs/api/data/samples.md — переписана. Период MetaTrader был указан как «Май 2025» (реестр врал, дока переписала), а рядом стоял пример записи с ценой `2425.15` — такой цены в датасете нет вовсе (диапазон `close` 3312.02 … 3405.58), то есть пример был от ещё более старой выгрузки. Три поколения метаданных в одном разделе, ни одно не совпадало с данными. Подпись «Версия BQuant: 1.0.0-dev» при пакете 0.0.9 убрана
[included] [Changed] docs/api/data/{schemas,validator}.md — переписаны. `schemas.md` честно предупреждала «текущая реализация — заготовка» и тут же печатала `res.is_valid` — оговорка верная и бесполезная. `validator.md` не печатала ничего: пять вызовов подряд без единого вывода, то есть проверить страницу было нечем
[not_included] [Technical] Три законных `False`, которые теперь объяснены на странице валидатора вместо того, чтобы выглядеть находками: `is_complete: False` (четыре колонки маркеров пусты целиком), `is_continuous: False` (482 часовые метки отсутствуют — рынок золота стоит по выходным), `detected_frequency: None` (из-за той же неравномерности). Заполнять такие пропуски значит рисовать торговлю там, где её не было
[not_included] [Technical] Четвёртый слой сработал третий раз за волну: переписывая `samples.md`, я потерял `SampleDataGenerator`. Восстановлен с содержанием — заодно там же записан остаток G40, потому что генератор и есть то место, где метаданные портятся
[not_included] [Technical] Числа снова писал по памяти и снова ошибался — четыре раза: `find_datasets` отдаёт отсортированный список (`['mt_xauusd_m15', 'tv_xauusd_1h']`, а я написал порядок реестра — на той же странице, где сам же и объяснил, что функция сортирует); `detect_market_sessions` добавляет `['session', 'london_ny_overlap']`, а не пять колонок сессий; `date_range` из `get_data_info()` содержит два ключа и `Timestamp`, а не три и строки; `len(gaps)` = 482, а не 12. Каждое поправлено прогоном
[not_included] [Technical] Проверка блока: слои доков **1750 проверок, 0 падений**; полный сьют **3079 passed, 36 skipped, 0 failed** (было 2976). Волна 3 — 17 страниц из 31
