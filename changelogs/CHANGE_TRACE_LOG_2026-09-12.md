# Change Trace Log — 2026-09-12

[Проверка трёх починок инструмента на своём дереве — третье подтверждение на третьем дереве]

[not_included] [Technical] Условия: `codmap==0.0.17` с PyPI в чистом venv 3.12, repo-scoped deep, `--repeat 3`, потребительские корни tests/examples/scripts/research. Граф на `925bdd5`: 5306 узлов / 14598 рёбер, тот же артефакт положен в `.codemap/`
[not_included] [Technical] `report impact` на `calculate_atr`, `--flow-depth 6`: **4 of 301 entry points**, четыре пресета шагом 6, раскладка внешних вызывающих `analyze_macd_zones` — examples×3, research×1, scripts×2, tests×37 = 43, совпала с замером инструмента до слагаемого. На умолчании глубины 5 — «0 of 301, но 4 достают дальше, ближайший на шаге 6»: пустота называет свой род вместо прежнего «no entry point reaches it»
[not_included] [Technical] Расхождение 302 (их замер) против 301 (наш) **не списано на коммит, а измерено**: пересобрали граф на их коммите `3591f83` той же версией — 4 of 302, число сходится ровно. Разница в одну единицу — наша правка `3b8b80a`, удалённый приватный `ValidationSuite._validate_result`, который сам был точкой входа. На fast ответ прежний (`run — step 3`, 1 of 331), то есть fast по-прежнему отвечает случайно правильной головой
[not_included] [Technical] `diff v0.0.16 → main`, обе стороны deep и обе собраны 0.0.17: **1 added, 0 removed, 0 changed** (было 47 added на 0.0.16 из-за функций тестов). Единственный добавленный публичный символ — `SwingMetrics.__post_init__`, наш G73; сверено с собственным знанием интервала. `--exit-code` rc 0
[not_included] [Technical] Фильтр по степени уверенности: CLI его не принимает (только сервер), проверяли через `codemap serve` обычным JSON. На `ZigZagSwingStrategy.calculate` без фильтра — `returned 2, total 2, dropped 0, by_grade {heuristic: 2}`; с `min_confidence=exact` — `returned 0, total 2, dropped 2`. Блок стоит и без фильтра, то есть состав виден до отбрасывания — снимает двусмысленность, ради которой мы писали (13 целей из 25 с нулём exact-вызывающих)
[not_included] [Technical] `codemap check --require-contract` 0.0.17 по нашему `codemap.toml`: rc 0, пять правил названы, 9 ленивых циклов объявлены несуженными, 0 импортов под `TYPE_CHECKING`. Порог «codmap ≥ 0.0.14» в AGENTS.md не трогаем
[not_included] [other] Ответ — `kogriv/codemap#19`. Отдельно передано наблюдение (не дефект): строка «Not judged» в `diff` называет **население** потребительских корней (1872 публичных символа в tests), а не дельту, и рядом с «1 added» читается как «1872 неразобранных изменения»
[not_included] [other] Единственное, чего мы снова не проверяли и о чём сказали прямо: «байты графа между версиями не двигаются» — пары «одно дерево двумя версиями» мы не строили

---

[Заведён трек пред-регистрации ожиданий; ожидания к шагу 2 выписаны ДО прогона]

[included] [Added] `devref/architecture/preregistered_expectations_backlog_2026-09.md` — бэклог из двух шагов: процедура пред-регистрации (§2) и сторож согласованности статусов (§3). Открытый вопрос §7.1 разбора готовности («не породит ли сторож третье место, где живёт статус») решён до кода: сторож не заводит хранилища и не объявляет истин — сверяет две уже существующие записи, источник истины остаётся один, реестр гэпов
[included] [Changed] `AGENTS.md` — правило в сжатом виде: прогон, чей результат предъявляется как доказательство, несёт выписанные заранее ожидания; хотя бы одно обязано провалиться, если ничего не делали

[ОЖИДАНИЯ] Выписаны 2026-09-12 до написания сторожа §3. Предмет: тест согласованности статусов + прогон затронутых слоёв. Сверка — следующим блоком, после прогона.

[not_included] [Technical] **Обязано измениться (1/5).** Появляется `tests/unit/test_status_claims_match_the_registry.py`. Общее число собранных тестов в `tests/unit/` растёт ровно на число функций этого файла; ни один существующий тест не меняет исход (не «зелёный», а именно **тот же** исход)
[not_included] [Technical] **Обязано остаться (2/5).** `tests/unit/test_docs_parity.py` = 546 passed (замер сегодня, 3.10 с). Три слоя доков вместе = 1350 passed (замер сегодня, 68 с). Обе цифры обязаны совпасть после правки; расхождение означает, что сторож задел парити, а не статусы
[not_included] [Technical] **Корпус не пуст (3/5) — положительный контроль формы.** Сканер обязан увидеть в реестре ≥ 60 различных G-номеров (сейчас их 76: G1–G74 плюс G10a/G10b/G13a) и ≥ 100 упоминаний G-номеров вне реестра. Ноль здесь означает зелёный вхолостую — ровно то, чем сторож был бы бесполезен
[not_included] [Technical] **Предсказание содержания (4/5) — то, ради чего список пишется до, а не после.** Утверждение А, «упомянут номер, которого в реестре нет»: ожидаю **0** нарушений. Утверждение Б, «статус номера вне реестра расходится с реестром»: ожидаю **1–3**, и называю кандидатов заранее — «остаток G40» в `READINESS_2026-09-10.md` §4.5 и формулировки про G38/G40/G47 как открытые, оставшиеся с 0.0.10. Утверждение В, «маркер 🟡 в шапке при строке о закрытии»: ожидаю **0** на дереве, но **ожидаю попадание самого бэклога, заведённого сегодня**, если правило написать грубо — это названный риск, а не сюрприз
[not_included] [Technical] **Отказ обязан прозвучать (5/5) — мутации.** (а) вписать в `tests/STATUS.md` упоминание несуществующего `G99` — тест обязан покраснеть и **назвать файл и номер**, а не просто упасть; (б) объявить в произвольном файле `devref/` закрытый `G74` открытым — красный по утверждению Б. Обе мутации обязаны быть красными до отката и зелёными после

[СВЕРКА] Прогон сделан 2026-09-12 после написания сторожа. По пунктам списка выше, без снятия пунктов задним числом.

[included] [Added] `tests/unit/test_status_claims_match_the_registry.py` — пять проверок: корпус не пуст (положительный контроль), упомянутый G-номер есть в реестре, живой документ согласен с реестром об открытости, живой документ вообще объявляет статус, документ с 🟡 не содержит строки о закрытии. Живое против замороженного берётся из уже существующего признака `**Тип:** живой …`; `devref/archive/`, `changelogs/` и сам реестр исключены, каждый со своей причиной
[included] [Changed] `devref/architecture/preregistered_expectations_backlog_2026-09.md` — §3 дополнен правилом живое/замороженное, добавлен §6 (итог); маркер шапки перевёрнут на ✅ **в этом же коммите**, что и итог — та самая ошибка, которую §4.4 разбора готовности поймал на прошлом бэклоге
[included] [Changed] `tests/STATUS.md` — фраза «сторожа, который сравнивал бы названное здесь открытым с реестром, нет» перестала быть верной в этом коммите и заменена описанием того, что сторож умеет, **и того, чего он не умеет** (смысла не читает)

[not_included] [Technical] **1/5 обязано измениться — сошлось.** `tests/unit`: собрано 2906 → 2911, пройдено 2872 → **2877 passed / 34 skipped**, ровно +5 = число функций нового файла. Скипы не изменились; ни один существующий тест не сменил исход (сверено двумя полными прогонами, с файлом и с `--ignore`)
[not_included] [Technical] **2/5 обязано остаться — сошлось.** `test_docs_parity.py` = 546 passed; три слоя доков вместе = 1350 passed. Обе цифры воспроизведены после правки
[not_included] [Technical] **3/5 корпус не пуст — сошлось.** 106 файлов в скане, **561** упоминание G-номеров, **76** строк реестра (у 75 есть маркер статуса). Живых документов 8, всегда-текущих 3
[not_included] [Technical] **4/5 предсказание содержания — ДВА ПРОМАХА ИЗ ТРЁХ, и оба информативнее сбывшихся пунктов.** Утверждение А («0 несуществующих номеров»): первый прогон дал **14 нарушений**, все про `G8`. Дефект оказался в стороже, не в дереве — реестр не одна таблица, в разделах-предложениях у строки нет колонки статуса; после разделения «номера нет» и «у строки нет маркера» — 0, как и предсказано. Утверждение Б («1–3 расхождения статусов»): **0**, и **0 же при снятом фильтре живое/замороженное** — промах не спасён сужением области, расхождений в дереве просто нет; ожидание опиралось на формулировку времён 0.0.10 («открыты G38 и остатки G40/G47»), устаревшую с тех пор, а «остаток G40» в роадмапе противоречием не является (реестр называет G40 закрытым с названным остатком). Утверждение В («0 на дереве, но своё попадание при грубом правиле»): 0 на дереве, своего попадания не было — и это оказалось хуже, см. сюрприз 1
[not_included] [Technical] **5/5 мутации — сошлось с находкой.** (а) упомянуть номер, которого нет в реестре — красная, называет файл и номер; (б) объявить закрытый `G74` открытым в живом документе — красная, называет файл, строку и маркер реестра; (в) дописать «✅ закрыт» в документ с 🟡 — **сначала зелёная**, см. сюрприз 1; после починки красная; (г) снять строку статуса у живого документа — красная. Все четыре зелёные после отката
[not_included] [Technical] **СЮРПРИЗ 1 (главный).** `**Статус:**` в шапках репозитория имеет две формы — в начале строки и после даты на той же строке. Якорь `^` в регулярке молча выключал проверку на документах второй формы, включая сам заводимый бэклог. Прогон был зелёным; красной проверку сделала только мутация (в). Ровно та форма дефекта, ради которой заводился трек: **пустота, похожая на результат**. Починено, добавлена пятая проверка — живой документ обязан объявлять статус, иначе он невидим тем же способом
[not_included] [Technical] **СЮРПРИЗ 2.** Первым настоящим уловом сторожа стал документ **про сторож**: в §6 бэклога стоял литерал несуществующего номера из описания мутации. Исключение заводить не стали (правило репозитория: у каждой ветки-исключения должен быть свой зонд) — литерал убран из прозы. Трейслог под правило не попадает: `changelogs/` исключён из скана как история, и это сработало так, как задумано
[not_included] [other] Чего сторож не умеет — сказано и в нём, и в `tests/STATUS.md`: смысла он не читает. Второе сентябрьское протухание (`STATUS` утверждал, что инвариантов нет, и рядом объяснял, как их запускать) этой проверкой не ловится

---

[Различимость за пределами флагмана — §7.3 разбора готовности. Область: визуализация зон]

[ОЖИДАНИЯ] Выписаны 2026-09-12 до прогона. Предмет: таблица рычагов пути визуализации зон — «включено ≠ выключено» по отпечатку фигуры. Приём тот же, что в G71 на билдере; область выбрана не наугад — под ней уже был G47, где `theme=` принимали три класса и не применял ни один, а светлая и тёмная темы давали побайтно одинаковую фигуру.

[not_included] [Technical] **Область и её размер (1/5).** 18 вызываемых целей: семь методов `ZoneVisualizer`, три зонных метода `FinancialCharts`, `plot_zigzag_verification` и пакетные обёртки. Рычагов с умолчанием — **55**, из них `title` — 13, нетитульных — **42**. Если харнесс отчитается о меньшем числе, он не позвал всё, и это дефект замера, а не пакета
[not_included] [Technical] **Положительный контроль первый (2/5).** Все **13** рычагов `title` обязаны различать. `title` — рычаг, про который мы знаем, что он работает; хоть один «идентично» здесь означает сломанный харнесс, а не находку в пакете
[not_included] [Technical] **Положительный контроль второй (3/5).** Отпечаток обязан совпасть сам с собой: два вызова с одинаковыми аргументами дают одинаковый хеш. Недетерминированная фигура сделала бы «различает» истинным всегда, то есть таблицу бесполезной
[not_included] [Technical] **Предсказание содержания (4/5).** Ожидаю **1–4** нетитульных рычага без различия. Кандидаты называю заранее, до прогона: `show_zone_stats` (стоит рядом с `show_zone_metrics` — два имени на одно понятие, один из них может быть мёртв), `aggregate_metrics_mode` (перечисление, возможно с единственной реализованной веткой), `swing_marker_size` (размер маркера может не доезжать до фигуры), `xaxis_num_ticks` (может игнорироваться при `time_axis_mode='dense'`), `indicator_chart_types`
[not_included] [Technical] **Отказ обязан прозвучать (5/5).** Рычаг, для которого честной альтернативы нет (нужны данные, которых в дереве нет, или значение зависит от другого рычага), обязан быть **назван непроверенным отдельной строкой**, а не посчитан различающим. Ноль непроверенных при 42 рычагах был бы подозрительным сам по себе

[СВЕРКА] Прогон таблицы рычагов сделан 2026-09-12 после написания замера, по пунктам списка выше.

[included] [Fixed] **G75** — `show_gap_lines` принимали и не рисовали. `plot_zones_on_price_chart` передавала флаг в `_create_plotly_zones_on_price`, где он был объявлен в сигнатуре и ни разу не прочитан в теле. Сэмпл 1000 баров, 77 зон, **44 разрыва**: `dense` sha `0c313fc3a97e5585` с флагом и без, `timeseries` sha `6943508dfec96ca7` с флагом и без — побайтно одно и то же. После правки 121 фигура вместо 77 (77 зон + 44 вертикали), sha `6d3cb7de7989bc45` и `cf9a8fe550aa5ea6`. Линии строятся в обоих режимах, потому что разрыв невидим в обоих: в `timeseries` вырезан `rangebreaks`, в `dense` ось позиционная
[included] [Added] `tests/visualization/test_levers_change_the_figure.py` — 4 проверки: оба режима оси (прирост `layout.shapes` равен числу разрывов), положительный контроль на кадре **без** разрывов, и зажим `xaxis_num_ticks`. Мутация (`if show_gap_lines:` → `if False:`) красит оба параметризованных случая, откат зелёный
[included] [Changed] Докстроки `plot_zones_on_price_chart`, `generate_dense_axis_labels` и `docs/api/visualization/zones.md` — выписан зажим `xaxis_num_ticks` и оговорка, что matplotlib-путь линий не рисует. `CHANGELOG.md`, реестр гэпов (строка G75), разбор `devref/gaps/visualization/g75_…`

[not_included] [Technical] **1/5 область и размер — сошлось, с названной недостачей.** Рычагов 55, как и выписано. Измерено **49**; остальные 6 — все рычаги `plot_zigzag_verification`, чей базовый вызов упал: фикстуре нужен вычисленный `SwingContext`, а прогон строился без области `global`. Названо непроверенным, не зачтено различающим
[not_included] [Technical] **2/5 положительный контроль первый — сошлось.** Все **12** измеренных рычагов `title` различают. Тринадцатый — в упавшей цели, см. выше
[not_included] [Technical] **3/5 положительный контроль второй — сошлось.** Ни одной недетерминированной фигуры: базовый отпечаток совпал сам с собой на всех 13 достижимых целях
[not_included] [Technical] **4/5 предсказание содержания — сырое число мимо, метод оказался неверным.** Ожидал 1–4 неразличающих рычага; сырой прогон дал **11**. Второй проход, где каждый подозреваемый мерился **с включённым своим гейтом**, показал: 8 из 11 не мертвы, а заперты — `swing_marker_size` за `show_swings=False`, `indicator_columns`/`indicator_chart_types` за `show_indicators=False`, `aggregate_metrics_mode` за `show_aggregate_metrics=False`, `show_zone_stats` за `show_zone_metrics=False`, `date_range` за `max_zones=5`. Настоящих осталось **2**, что в предсказанный диапазон попадает — но попало бы и при полностью неверном разборе
[not_included] [Technical] **4/5, продолжение — счёт по названным кандидатам.** Пять кандидатов были выписаны до прогона и все пять оказались в сыром списке «идентично» (5 из 5). После разделения «мёртв» против «заперт» из них выжил один — `xaxis_num_ticks` в `plot_zone_detail`. **Настоящего дефекта (`show_gap_lines`) в списке кандидатов не было.** То есть предсказание угадало шум и не угадало находку
[not_included] [Technical] **5/5 отказ прозвучал — сошлось.** 6 рычагов названы непроверенными (`column_schema` и `columns` в трёх методах — честной альтернативы без чужой схемы колонок нет), плюс упавшая цель. Ноль непроверенных, которого следовало бы опасаться, не случился
[not_included] [Technical] **СЮРПРИЗ (методологический, важнее находки).** Замер «по одному рычагу от умолчаний» **не отличает мёртвый рычаг от запертого**: 8 из 11 «смертей» оказались гейтами, выключенными по умолчанию. Таблица рычагов обязана иметь второй проход с включёнными гейтами, иначе она производит восемь ложных дефектов на два настоящих. Это касается и приёма из G71 — там гейтов не было, и потому не всплыло
[not_included] [Technical] **Второй вердикт, не дефект:** `xaxis_num_ticks` не различает, потому что фактическое число меток = `max(8, min(запрос, ideal, точек))`. Нижний порог 8 — решение о читаемости; неверным было молчание о нём: вызывающий передавал 3 и получал 8, ничего об этом не узнав. Зажим выписан в две докстроки и доку и закреплён тестом — исправлена **тишина**, а не поведение
[not_included] [Technical] **Прогон после правки:** `tests/visualization` + `tests/unit` — **2902 passed / 34 skipped** (было 2877 по одному `tests/unit`; +21 существующих визуализации, +4 новых). Слои доков: парити 546, публичная поверхность 228, исполнение примеров **576** — без изменений к замеру этого же дня

---

[Различимость, область вторая: статистические тесты. §7.3, продолжение]

[ОЖИДАНИЯ] Выписаны 2026-09-12 до прогона. Предмет: таблица рычагов `bquant.analysis.statistical`. Отличие от первой области: отпечатком служит возвращаемое значение, а не фигура, и мёртвый рычаг здесь даёт не картинку, а **число, поданное как результат**.

[not_included] [Technical] **Область и её размер (1/6).** 9 вызываемых целей, **28** рычагов. Исключены и названы: `get_logger` (переэкспорт из `core`, не предмет) и девять полей-параметров трёх контейнеров результата (`AnalysisResult`, `HypothesisTestResult`, `RegressionResult`) — это поля, а не ручки поведения. Меньшее число в отчёте харнесса = он не позвал всё
[not_included] [Technical] **Положительный контроль первый — детерминизм (2/6).** Один и тот же вызов дважды обязан дать одинаковый отпечаток на всех целях. Иначе «различает» истинно всегда
[not_included] [Technical] **Положительный контроль второй — чувствительность к данным (3/6).** Тот же вызов на **другой выборке** обязан дать другой отпечаток. Цель, отвечающая одинаково на разных данных, означает либо слепой отпечаток, либо функцию, не читающую свой вход, — и второе было бы находкой крупнее любого рычага
[not_included] [Technical] **Пороговые рычаги мерить против измеренного p (4/6).** `alpha` не меняет p-value, она меняет вердикт. Альтернативы выбираются **по измеренному p** так, чтобы порог его пересекал; если такой пары не существует (p ровно 0 или 1), рычаг называется непроверенным, а не различающим
[not_included] [Technical] **Предсказание содержания (5/6).** После прохода с включёнными гейтами ожидаю **2–5** неразличающих рычагов. Кандидаты называю заранее: `vocabulary=None` в четырёх местах, `name='data'` в `descriptive_statistics` (может не доезжать до вывода), `seed=0` в `lilliefors_normal`, `alpha` в конструкторах `HypothesisTestSuite`/`ZoneRegressionAnalyzer` (может не доходить до вердиктов, ср. G63). Отдельно предсказываю, что `tolerance_pct` окажется **заперт** гейтом `price_levels=None`, а не мёртв
[not_included] [Technical] **Отказ обязан прозвучать (6/6).** `config=None` в `StatisticalAnalyzer.__init__` — непрозрачный словарь; без знания ключей честной альтернативы нет, и он обязан быть назван непроверенным. Ноль непроверенных при 28 рычагах был бы подозрительным

[СВЕРКА] Прогон таблицы рычагов статистики сделан 2026-09-12, по пунктам списка выше.

[included] [Fixed] **G76** — `ZoneRegressionAnalyzer(alpha=…)` решал только, писать ли строку в лог. `self.alpha` считал значимые предикторы и выбрасывал их в `logger.info`; в `RegressionResult` вердикт не попадал. `alpha` 0.001 и 0.5 давали один отпечаток `3d25c5550d5145e5` у `explain_zone_duration` и такой же эффект у `explain_price_return`. Теперь `metadata['alpha']` и `metadata['significant_predictors']` заполняются у обеих моделей — аддитивно, форма результата не менялась
[included] [Added] `tests/analysis/test_statistical_levers_change_the_answer.py` — 4 проверки: оба метода различают `alpha` (строгий набор обязан быть **подмножеством** мягкого), положительный контроль «`p_values` и `r_squared` от `alpha` не зависят» (иначе прошла бы реализация, переподгоняющая модель под каждый `alpha`), и закрепление того, что `name` в `descriptive_statistics` — метка лога. Мутация (убрать обе записи в `metadata`) красит оба параметризованных случая
[included] [Changed] Докстроки `descriptive_statistics` и конструктора `ZoneRegressionAnalyzer`, `docs/api/analysis/statistical.md`, `CHANGELOG.md`, реестр (G76), разбор `devref/gaps/statistical/g76_…`

[not_included] [Technical] **1/6 область и размер — сошлось.** 28 рычагов на 9 целях, как выписано; исключения (`get_logger` и девять полей трёх контейнеров результата) названы заранее и не всплыли задним числом
[not_included] [Technical] **2/6 детерминизм — не сошлось с первого раза, и это работа контроля.** Три цели (`run_all_tests`, оба `explain_*`) дали разный отпечаток на двух одинаковых вызовах. Причина — `metadata['timestamp']` с настенными часами: законная провенанс-запись, а не дефект. Харнесс стал исключать волатильные ключи; **без этого контроля вся таблица была бы «различает» на любом рычаге**
[not_included] [Technical] **3/6 чувствительность к данным — поймала две ошибки замера, не пакета.** `correlation_analysis(x,y)` против `(y,x)` — корреляция симметрична, отпечаток обязан совпасть; `test_normality` на двух нормальных выборках возвращает `True` и там, и там. Обе «слепоты» были моими, а не пакета; после смены альтернатив (независимая пара, экспоненциальная выборка) все 5 целей чувствительны
[not_included] [Technical] **4/6 пороги против измеренного p — правило окупилось дважды.** `test_normality(alpha=…)` и `lilliefors_normal(seed=…)` выглядели мёртвыми: на сильно ненормальной выборке p упирается в разрешение метода (0.0005 при 2000 симуляциях), и ни порог, ни зерно ничего не меняют. На выборке с p=0.124 оба различают (зёрна дают 0.1244/0.1179/0.1174/0.1164; пороги 0.01 и 0.15 переворачивают вердикт). Отдельно: `normality_test` **отказывается вслух** на `alpha` вне таблицы Андерсона-Дарлинга — 1e-9 даёт `ValueError` с перечнем допустимых, а не молчаливый ноль
[not_included] [Technical] **5/6 предсказание — 2 мёртвых при ожидании 2–5, оба из списка кандидатов.** Названы были пять: `vocabulary` (×4) — **жив**, без него три теста отказываются вслух, с ним считают; `name` в `descriptive_statistics` — **верно**; `seed` — **неверно**, данные, а не дефект; `alpha` конструктора `HypothesisTestSuite` — **жив**; `alpha` конструктора `ZoneRegressionAnalyzer` — **верно**. Предсказание «`tolerance_pct` заперт гейтом `price_levels=None`, а не мёртв» — сбылось. Контраст с первой областью: там настоящей находки в списке кандидатов не было, здесь — обе
[not_included] [Technical] **6/6 отказ прозвучал.** `config=None` в `StatisticalAnalyzer.__init__` назван непроверенным: без знания ключей честной альтернативы нет. Кроме него непроверенным остался один вызов из-за моей ошибки в имени типа теста (`zone_duration` вместо `duration`) — исправлено в проходе 2, не списано
[not_included] [Technical] **Прогон после правки:** `tests/unit` + `tests/analysis` + `tests/visualization` + `tests/integration` — **3547 passed / 34 skipped** (7:19). Слои доков внутри: парити 546, публичная поверхность 228, исполнение примеров 576

---

[Различимость, область третья: загрузчики, processor, validator. §7.3, окончание]

[ОЖИДАНИЯ] Выписаны 2026-09-12 до прогона. Ставка области: рычаг чистки или ресемплинга кормит **всё, что ниже по течению**, поэтому мёртвый рычаг здесь дороже, чем в двух предыдущих областях.

[not_included] [Technical] **Область и её размер (1/6).** 17 вызываемых целей, **32** рычага: `loader` 11 (`data_dir` ×3, `data_source`/`quote_provider` ×2 пары, `symbol`/`timeframe`/`validate_data` у `load_ohlcv_data`, `timeframe` у `load_xauusd_data`), `processor` 17 (`period`, `fill_method`/`remove_outliers`/`outlier_threshold`, `timezone`, `base_column`/`method`, шесть у `prepare_data_for_analysis`, `columns`/`threshold`/`method`, `method` у `resample_ohlcv`), `validator` 4. Исключены и названы: `create_data_validation_error` (5 полей исключения, переэкспортирован в трёх модулях) и `get_logger` (переэкспорт из `core`)
[not_included] [Technical] **Положительный контроль первый — детерминизм (2/6).** Одинаковый вызов дважды обязан дать одинаковый отпечаток. Волатильные ключи провенанса (как `timestamp` в прошлой области) исключаются из отпечатка **явным списком**, а не молча
[not_included] [Technical] **Положительный контроль второй — чувствительность к данным (3/6).** Тот же вызов на другом сэмпле обязан дать другой отпечаток. Цель, отвечающая одинаково на разных данных, — находка крупнее любого рычага
[not_included] [Technical] **Рычаги мерить на данных, где им есть что делать (4/6).** Правило, окупившееся в прошлой области дважды: `outlier_threshold` на кадре без выбросов не различает по устройству данных, `strict` на исправном кадре даёт тот же вердикт, `lag_periods` заперт `create_lags=False`. Для каждого такого рычага строится кадр, на котором он **обязан** сработать; если построить нельзя — рычаг называется непроверенным, а не различающим
[not_included] [Technical] **Предсказание содержания (5/6).** Ожидаю **2–5** мёртвых рычагов после прохода с гейтами. Кандидаты называю заранее: `symbol` и `timeframe` в `load_ohlcv_data` (подозрение то же, что оправдалось на `name` в `descriptive_statistics` — метка, не доезжающая до результата), `method` в `normalize_prices` и в `remove_price_outliers` (перечисления, у которых может быть реализована одна ветка), `timezone` в `detect_market_sessions`, `fill_method` в `clean_ohlcv_data` (переписан после G50, мог остаться одноветочным). Отдельно предсказываю, что `lag_periods` окажется **заперт**, а не мёртв
[not_included] [Technical] **Отказ обязан прозвучать (6/6).** `data_dir` у трёх функций и `data_source`/`quote_provider` требуют файловой раскладки, которой в дереве нет; ожидаю, что часть из них будет названа непроверенной. Ноль непроверенных при 32 рычагах был бы подозрительным

[СВЕРКА] Прогон таблицы рычагов данных сделан 2026-09-12, по пунктам списка выше. Три находки — больше, чем в двух предыдущих областях вместе.

[included] [Fixed] **G77** — `get_data_path` проверял `timeframe` и отказывал со списком, а `data_source`/`quote_provider` подменял через `.get(x, fallback)`: `does_not_exist` → generic-путь, `nonexistent` провайдер → путь OANDA. Опечатка в одну букву возвращала законный путь к чужому файлу. Там же `load_ohlcv_data` ловил отказ `validate_timeframe` в `warning`. Оба аргумента теперь отказывают с перечнем; проверка таймфрейма вынесена **за** `try` — внутри она стала бы `DataLoadingError`, а это не отказ загрузки
[included] [Fixed] **G78** — `validate_time_series_continuity` считала разрывы только внутри `if expected_frequency:`; по умолчанию `is_continuous` оставался `True` из инициализации словаря. Явный вызов на часовом сэмпле находит **482** пропущенные метки, вызов по умолчанию находил **0**. Теперь сетка выводится по медианному шагу индекса и сходится с объявленной: 482 против 482 (1h), 428 против 428 (m15); поле `gap_basis` различает «мерили по переданной», «мерили по выведенной» и **`None` — не мерили**
[included] [Fixed] **G79** — `resample_ohlcv(method=…)` удалён: документировался как ('standard','custom'), в теле встречался один раз, в словаре контекста ошибки; ветки `custom` не было никогда, вызовов с ним в дереве нет. Попутно выписана область действия `normalize_prices(base_column=…)` — только `first_value`
[included] [Added] `tests/unit/test_data_levers_change_the_answer.py` — 9 проверок с двумя положительными контролями (известный провайдер обязан давать **другой** путь; кадр без разрывов обязан остаться непрерывным). Три мутации красят пять проверок
[included] [Changed] `docs/api/data/validator.md`, `processor.md`, `loader.md`, `CHANGELOG.md`, реестр (G77–G79), три разбора в `devref/gaps/data/`

[not_included] [Technical] **1/6 область и размер — сошлось.** 32 рычага на 17 целях; исключения (`create_data_validation_error`, `get_logger`) названы заранее
[not_included] [Technical] **2/6 детерминизм — сошлось.** Ни одной недетерминированной цели
[not_included] [Technical] **3/6 чувствительность к данным — НАШЛА ДЕФЕКТ, а не ошибку замера.** `validate_time_series_continuity` отвечала одинаково на часовом и пятнадцатиминутном сэмплах. В прошлой области этот контроль ловил мои промахи; здесь он поймал G78 — самую дорогую находку дня. Контроль, заведённый как страховка замера, сработал как детектор
[not_included] [Technical] **4/6 рычаг мерить там, где ему есть что делать — снова окупилось.** Заперты данными и живы при открытом гейте: `strict` (нужен кадр без обязательной колонки), `required_columns` (нужна колонка вне умолчаний), `lag_periods` (`create_lags=False` — предсказано заранее), `validate_data` (на исправном файле разницы нет по устройству: валидация не меняет данные, она отказывает)
[not_included] [Technical] **5/6 предсказание — 2 из 5 кандидатов верны, но найдено ТРИ дефекта, и два не были предсказаны.** Кандидаты: `symbol`/`timeframe` у `load_ohlcv_data` — половина верна (`symbol` документирован как метка логов, это не дефект; `timeframe` оказался дефектом, но не тем, что предсказывал — не мёртв, а понижен до warning); `method` у `normalize_prices` — **неверно**, отказывает вслух; `method` у `remove_price_outliers` — **неверно**, обе ветки живы (разделить на этих данных не удалось, записано как «не разделено», а не «мёртв»); `timezone` — **неверно**, различает; `fill_method` — **неверно**, три живые ветки. Верным оказался один: `resample_ohlcv(method=…)`, которого в списке не было вовсе. Предсказание «`lag_periods` заперт» — сбылось
[not_included] [Technical] **СЮРПРИЗ (методологический, второй за день).** Первый отпечаток кадра брался по `head(5)`/`tail(5)` + форма + колонки — и дал **ложное «идентично»** для `fill_method`, чей единственный эффект лежит в середине кадра (строки 10–11). Грубый отпечаток производит ложные смерти так же, как невключённый гейт: обе ошибки дают «рычаг не действует» там, где он действует. Отпечаток заменён на хеш **полного содержимого**; после замены `fill_method` различает все три ветки
[not_included] [Technical] **Прогон после правки:** `tests/unit` + `analysis` + `visualization` + `integration` — **3557 passed / 34 skipped** (7:07), было 3547; +10 собранных проверок нового файла (восемь функций, одна параметризована вдвое). Слои доков внутри без изменений

[included] [Changed] `tests/STATUS.md` — критерий 12 закрыт уже не только на флагмане: к G71 добавлены три области (G75–G79). Правка в том же коммите, что и итог, а не «потом»
[included] [Changed] `devref/architecture/package_roadmap_2026-07.md` §3.5 — §7.3 разбора готовности закрыт: три области, 115 рычагов, пять находок, и два методологических ограничения замера записаны там же. `READINESS_2026-09-10.md` **не трогали**: он датированный снимок, и правка в нём была бы подделкой даты

[Точка входа `python -m bquant` — решение владельца от 2026-09-12]

[included] [Added] `bquant/__main__.py` — вызывает ту же `bquant.cli.main`, что и консольный скрипт. Своего разбора аргументов не держит намеренно: второй литерал, обязанный совпадать с первым, — форма дефекта, закрытая в этом проекте трижды (G8, G32, G33). До этого `python -m bquant` давал `rc 1` и `No module named bquant.__main__` — сообщение, которое читается как «пакет сломан», а не «форма не поддерживается»
[included] [Added] `tests/unit/test_module_entry_point.py` — 2 проверки: подпроцесс `python -m bquant --help` даёт rc 0 и ту же строку `usage`, что и `build_parser()`; модуль **делегирует** (его `main` — это объект `bquant.cli.main`, в исходнике нет ни `argparse`, ни `add_argument`). Две мутации: убрать модуль — красная на сборе; оставить модуль, но не звать `main` — красная на пустом выводе. Обе зелёные после отката
[included] [Changed] `docs/user_guide/cli.md` и `CHANGELOG.md` — форма `-m` названа как та же точка входа, а не как вторая

---

[Иерархия исключений в `core/config.py` — последняя известная ломающая правка перед релизом]

[ОЖИДАНИЯ] Выписаны 2026-09-12 до правки. Повод: пункт §4 бэклога P2-A, отложенный 28.08, и выросший с 5 мест до 7 (два добавил G77 сегодня).

[not_included] [Technical] **Замер объёма — сделан до решения, и он переопределил задачу (1/6).** `raise ValueError` во всём пакете — **166** в 20+ файлах; в `bquant/core/config.py` — **7**. Бэклог называл только второе число. Полная замена по пакету — работа другого порядка и ломает всё сразу; в этой правке делается **названный пункт**, а пакетный вопрос заводится отдельным решением с измеренным числом, а не подразумевается сделанным
[not_included] [Technical] **Обязано измениться (2/6).** В `core/config.py` не остаётся ни одного `raise ValueError`: `validate_timeframe` поднимает `InvalidTimeframeError` (класс определён ровно под это и до сих пор **не имел ни одного вызывающего**), остальные шесть — `ConfigurationError`. Тексты сообщений сохраняются дословно, чтобы совпадение по тексту у вызывающего продолжало работать
[not_included] [Technical] **Обязано остаться (3/6).** `InvalidTimeframeError` — подкласс `ConfigurationError` → `BQuantError` → `Exception`, и **не** подкласс `ValueError`. Двойное наследование от `ValueError` рассматривалось и отвергнуто: оно сохранило бы чужие `except ValueError`, но это смягчение перехода, которых проект не делает. Значит, отказ обязан **перестать ловиться** прежним `except ValueError` — и это надо показать проверкой, а не предположить
[not_included] [Technical] **Предсказание содержания (4/6).** Ожидаю, что сломается **2–5 мест**: `bquant/data/loader.py` (единственный `except ValueError` в пакете, стоящий вокруг вызова config), `tests/unit/test_core_modules.py:315` и мой собственный `tests/unit/test_data_levers_change_the_answer.py`. Остальные 8 `except ValueError` в пакете и 106 из 108 `pytest.raises(ValueError)` в сьюте к config отношения не имеют и обязаны остаться зелёными. Число падений до сопутствующих правок — проверяемое предсказание
[not_included] [Technical] **Что НЕ входит и почему (5/6).** В `core/exceptions.py` лежат три валидатора без единого вызывающего (`validate_timeframe`, `validate_indicator_parameters`, `validate_ohlcv_data`), два из них — тёзки живых функций из других модулей. Это мёртвый код плюс коллизия имён, но **обе коллизии задокументированы намеренно** (`docs/api/core/exceptions.md` прямо предупреждает «вторая `validate_timeframe`, с которой не надо путать»). Удаление документированной поверхности — отдельное решение, а не попутная правка; заводится вопросом, не делается молча
[not_included] [Technical] **Отказ обязан прозвучать (6/6).** Мутация: вернуть `raise ValueError` в `validate_timeframe` — сторож обязан покраснеть на типе, а не на тексте сообщения (иначе он проверяет формулировку, а не иерархию)

[СВЕРКА] Правка сделана 2026-09-12 после замера объёма, по пунктам списка выше.

[included] [Fixed] **G80 (ЛОМАЮЩЕЕ)** — семь голых `raise ValueError` в `bquant/core/config.py` заменены иерархией: `InvalidTimeframeError` у `validate_timeframe` (класс определён ровно под этот отказ и до сих пор не имел **ни одного** вызывающего), `ConfigurationError` у двух отказов `get_data_path` и четырёх установщиков каталогов. Детали отказа кладутся в `details`; тексты сообщений сохранены дословно
[included] [Added] `tests/unit/test_config_refuses_with_the_project_hierarchy.py` — 7 проверок, включая **ast-скан модуля** (ни одного `raise` встроенного исключения — это то, что не даёт отклонению вырасти обратно по месту за раз), проверку того, что прежний `except ValueError` **больше не ловит** (цена названа проверкой, а не подразумевается), и положительный контроль на исправном вызове. Мутация красит три проверки, и красит **на типе**, а не на тексте
[included] [Changed] `tests/unit/test_core_modules.py`, `tests/unit/test_data_levers_change_the_answer.py`, `docs/api/core/config.md` — перехват переведён на иерархию; `CHANGELOG.md` (раздел ломающего), реестр (G80), разбор `devref/gaps/core/g80_…`; пункт §4 бэклога P2-A закрыт, пакетный вопрос оставлен открытым с измеренным числом

[not_included] [Technical] **1/6 замер объёма переопределил задачу — как и был заведён.** 166 `raise ValueError` по пакету против 7 в названном модуле. Сделан названный модуль; пакетный вопрос заведён решением владельца, а не подразумевается сделанным
[not_included] [Technical] **2/6 и 3/6 — сошлись.** В `config.py` не осталось ни одного `raise` встроенного исключения; `InvalidTimeframeError` — подкласс `ConfigurationError` → `BQuantError` и **не** подкласс `ValueError`, что показано проверкой, а не объявлено
[not_included] [Technical] **4/6 предсказание — 6 падений при ожидании 2–5, и промах двусторонний.** Предсказанное и не сбывшееся: `bquant/data/loader.py` **не** сломался — его `except ValueError` стоит за условием `if timeframe in SUPPORTED_TIMEFRAMES`, поэтому отказ там не возникает вовсе (мёртвая ветка перехвата, о которой я не знал). Непредсказанное и сбывшееся: доковый пример `docs/api/core/config.md:96` ловил `ValueError` — слой «поведение» его исполняет, и он покраснел
[not_included] [Technical] **СЮРПРИЗ, и он про мою работу, а не про код.** Шестое падение — `test_layers_point_one_way.py`, и не на этой правке: сторож слоёв покраснел на `bquant/__main__.py`, добавленном часом раньше. При `exhaustive = true` нераспределённый модуль в корне пакета обязан ломать контракт архитектуры — и сломал. Причина в порядке работы: после добавления `__main__.py` я прогнал его собственный тест и три слоя доков, но **не сторож архитектуры**, и правка ушла в пуш красной. Исправлено здесь же: `__main__` объявлен слоем (самым верхним — зовёт только `cli`) в тесте и в `codemap.toml`. Урок: новый модуль в корне пакета трогает контракт, даже если в нём три строки
[not_included] [Technical] **Контракт codemap на этой правке не перепроверялся** и сказано это прямо: граф в `.codemap/` собран в 10:18 на `925bdd5`, до появления `__main__.py`; локально `codmap` не установлен. Ast-сторож слоёв зелёный и проверяет те же правила; `codemap check` войдёт в релизный гейт на свежем графе
[not_included] [Technical] **Прогон после правки:** `tests/unit` + `analysis` + `visualization` + `integration` — **3568 passed / 34 skipped** (6:28), было 3557; +7 проверок нового сторожа иерархии, +4 от параметризации существующих. Красных нет

---

[Релизный гейт 0.0.19 на `a5120d3`]

[ОЖИДАНИЯ] Выписаны 2026-09-12 до прогона гейта. Это тот самый случай, ради которого заводилась процедура: прогон, результат которого будет предъявлен лабе как основание поднять пин.

[not_included] [Technical] **Обязано совпасть с локальным замером (1/5).** Сьют по всему `tests/` — **3605 собранных**, из них **3571 passed / 34 skipped**, и **на обоих плечах pandas** (3.0.5 и 2.3.3), в чистом клоне. Локально по четырём каталогам было 3568 passed / 34 skipped при 3602 собранных; разница в три теста — каталог `tests/performance`, который локально не гонялся
[not_included] [Technical] **Расхождение по пропускам — находка про окружение, а не про пакет (2/5).** Если число skipped на стенде отличается от 34, это означает, что там нет библиотеки, которая есть локально (или наоборот), и подлежит разбору **до** релиза, а не спискам «известных»
[not_included] [Technical] **Обязано остаться (3/5).** Батарея скриптов — **36 из 36 с rc 0** (`examples/*.py` и `research/notebooks/*.py --no-trap`, под `MPLBACKEND=Agg`). Число 36 держится с 0.0.13; рост означал бы новый скрипт, падение — потерянный
[not_included] [Technical] **Контракт архитектуры на СВЕЖЕМ графе (4/5).** `codemap check --require-contract` — rc 0, и в объявленных слоях их **семь**, а не шесть: `__main__` добавлен сегодня. Семёрка здесь работает положительным контролем свежести графа: на старом артефакте её быть не может
[not_included] [Technical] **Положительный контроль пересборки, и его слабая половина названа (5/5).** Символы, появившиеся сегодня, — `ZoneVisualizer._gap_boundaries` и `ZoneVisualizer._add_gap_lines` — обязаны быть в графе. Обратной половины («символ, удалённый новыми коммитами, обязан отсутствовать») **на этот раз нет**: за день не удалён ни один символ, только параметр `resample_ohlcv(method=…)`, а параметр не узел графа. Поэтому вторым различителем взят счёт слоёв из пункта 4 — он специфичен именно сегодняшнему дереву

[СВЕРКА] Гейт прогнан 2026-09-12 на `df7837e` (последний функциональный коммит — `a5120d3`; `df7837e` добавляет только блок ожиданий в трейслог). Чистый клон, оба плеча параллельно.

[not_included] [Technical] **1/5 сьют — сошлось до числа.** Плечо pandas 3.0.5: **3571 passed / 34 skipped**, rc 0, 9:59. Плечо pandas 2.3.3: **3571 passed / 34 skipped**, rc 0, 8:58. Предсказано было ровно 3571/34 на обоих — сошлось, включая разницу в три теста против локального прогона (`tests/performance`)
[not_included] [Technical] **2/5 пропуски — расхождения нет.** 34 на обоих плечах и 34 локально: окружения стенда и рабочей машины по составу библиотек совпадают. Разбирать нечего
[not_included] [Technical] **3/5 батарея — сошлось.** `DONE n=36 nonzero=0`: 36 скриптов из 36 с кодом возврата 0, под `MPLBACKEND=Agg`
[not_included] [Technical] **4/5 контракт на свежем графе — сошлось, и семёрка сработала как контроль.** `codemap check --require-contract` rc 0: «Contract satisfied. Rules enforced: **layered (7)**, forbidden (8), no_cycles, no_type_only_cycles, exhaustive». Семь слоёв вместо шести — то самое, чего на старом графе быть не может, то есть граф действительно пересобран. 9 ленивых циклов объявлены несуженными, 0 импортов под `TYPE_CHECKING`
[not_included] [Technical] **5/5 положительный контроль символами — сперва НЕ ОТРАБОТАЛ, и это моя ошибка в скрипте.** Звал `codemap search`, которого в CLI нет (есть `query`); обе строки в логе — тексты ошибок аргументов, а не результат. Перегнал отдельно: `_gap_boundaries` и `_add_gap_lines` — `defined_at` непустой (появились сегодня), `_validate_result` — `defined_at: []` (удалён на `3b8b80a`). **Уточнение к самим ожиданиям:** я написал, что обратной половины контроля нет, потому что сегодня ничего не удалено. Кандидат нашёлся — но он различает этот граф только с графами старше `3b8b80a`, а с предыдущим (`925bdd5`) не различает. То есть формулировка ожидания была верной по существу, а найденный кандидат слабее, чем выглядит
[not_included] [Technical] **Чего гейт не проверял и о чём сказано прямо:** установочная проверка из нейтрального cwd (ставится колесо с PyPI — возможна только **после** публикации); досыпка dev-зависимостей в окружения стенда не отработала (`No module named pip`: venv управляются uv, не pip). Сегодня это ничего не скрыло — новых dev-зависимостей не добавлялось, и сбор дал 3605 тестов, столько же, сколько локально. Но если бы добавилась, гейт бы соврал: это надо чинить до следующего релиза

[not_included] [other] Кандидат 0.0.19 предъявлен внешнему потребителю до релиза, а не после: issue №15 в его трекере со сха сборки `a5120d3`, числами гейта, разбором четырёх ломающих правок и **замером по его дереву** (греп по всем изменённым именам: ни одна из четырёх его пути не касается; предел замера — динамический вызов и перехват выше по стеку — назван прямо). Версия не бампалась: релиз — его решение

---

[Хвосты: замер стоимости старта и починка гейта]

[not_included] [Technical] **Замер старта — вопрос «CLI или импорт» закрыт числом.** `python -c "pass"` 0.01 с; `import bquant` **0.48–0.72 с**; `bquant --help` **3.94–3.98 с**. Разложение по модулям: `bquant.cli` 3.92 с, и почти всё это — `bquant.visualization` 3.89 с; внутри неё **`seaborn` 2.11 с**, `matplotlib.pyplot` 0.49 с, `plotly.graph_objects` 0.07 с. **Потребитель четырёх секунд не платит:** `import bquant` визуализацию не тянет (0.55 против 3.89). Четыре секунды — цена CLI, и она не про CLI, а про `seaborn` в пробе доступности библиотек
[not_included] [Technical] **Из чего состоит эта цена.** `import seaborn` стоит в **пяти** модулях (`visualization/__init__.py`, `charts.py`, `themes.py`, `zones.py`, `statistical.py`), а вызовов `sns.` в пакете **два** — оба в `statistical.py` (heatmap и boxplot), и у второго уже есть ветка «простая реализация без seaborn». То есть 2.1 с на старте платятся за две строки в одном модуле

[ОЖИДАНИЯ] Выписаны 2026-09-12 до правки импорта `seaborn`. Приём тот же, что в G64: внешняя библиотека грузится по первому запросу, а не на импорте.

[not_included] [Technical] **Обязано измениться (1/4).** `import bquant.visualization` — сейчас 3.89 с, ожидаю **≤ 2.2 с**; `bquant --help` — сейчас 3.94–3.98 с, ожидаю **≤ 2.3 с**. Числа снимаются той же командой, что и до правки
[not_included] [Technical] **Обязано остаться (2/4).** `import bquant` — 0.48–0.72 с, меняться не обязан (визуализацию он и так не тянет). Сьют по четырём каталогам — **3568 passed / 34 skipped**, без изменений. Обе функции, реально зовущие `sns` (`create_correlation_matrix` и `create_box_plot` на matplotlib-бэкенде), продолжают строить фигуру — на них есть тесты
[not_included] [Technical] **Отказ обязан прозвучать (3/4).** Сейчас флаг называется `MATPLOTLIB_AVAILABLE`, а поднимается только если импортировались **и** matplotlib, **и** seaborn: имя обещает одно, значит другое. После правки — два раздельных признака, и ветка без seaborn выбирается по своему признаку, а не по чужому
[not_included] [Technical] **Положительный контроль (4/4).** Снять `seaborn` из окружения: `import bquant.visualization` обязан пройти (сейчас он от seaborn зависит жёстко), обе функции — построить фигуру запасной веткой, сьют — остаться зелёным. Без этого контроля «ленивый импорт» неотличим от «перенесли строку в другое место»

[СВЕРКА] Правка импортов сделана 2026-09-12. Предсказание по причине оказалось неверным, и это главное, что стоит из неё вынести.

[included] [Changed] `bquant/visualization/statistical.py` — удалён `import plotly.figure_factory as ff`: он **ни разу не использовался** в пакете и тянул за собой ранний `plotly.graph_objs` целиком. `seaborn` переведён на ленивый импорт (`_seaborn()`), признаки `matplotlib` и `seaborn` разделены: флаг назывался `MATPLOTLIB_AVAILABLE`, а поднимался только если импортировались оба, и отсутствие seaborn читалось как отсутствие matplotlib
[included] [Changed] `bquant/visualization/{__init__,charts,themes,zones}.py` — снят `import seaborn as sns` (в этих четырёх модулях он не вызывался ни разу); `plotly.io` в `themes.py` переведён на ленивый `_plotly_io()`
[included] [Added] `tests/unit/test_visualization_does_not_pay_for_what_it_does_not_use.py` — 4 проверки: после `import bquant.visualization` в `sys.modules` **нет** `plotly.figure_factory` и `seaborn`; положительный контроль (зонд обязан видеть то, что действительно загружено — `plotly` и сам пакет); ленивое не значит потерянное (`_seaborn()` отдаёт модуль, когда он установлен). Утверждается **свойство, а не секундомер** — по той же причине, по которой `test_benchmark_global_vs_perzone` заменили счётными проверками. Мутация (вернуть мёртвый импорт) красит проверку

[not_included] [Technical] **1/4 обязано измениться — один порог взят, второй нет.** `bquant --help`: 3.94–3.98 → **2.16 с** (ожидание ≤ 2.3 — сошлось). `import bquant.visualization`: 3.89 → **2.42 с** (ожидание ≤ 2.2 — **не сошлось**)
[not_included] [Technical] **2/4 обязано остаться — сошлось.** `import bquant` 0.53 с, как и было: потребитель за это не платил и не платит. Сьют **3568 → 3573 passed / 34 skipped**, и дельта объяснена точно: +4 теста нового файла и +1 потому, что сторож молчаливых пропусков параметризован **по файлам тестов**, а файлов стало на один больше
[not_included] [Technical] **3/4 ПРЕДСКАЗАНИЕ ПРИЧИНЫ БЫЛО НЕВЕРНЫМ.** Я назначил виновником `seaborn`, померив его в одиночку: `import seaborn` действительно 2.11 с. Но в контексте пакета его цена перекрывается matplotlib/scipy/pandas, которые грузятся всё равно, и ленивый seaborn выиграл **около нуля**. Настоящая причина нашлась только через `python -X importtime`: **мёртвый `import plotly.figure_factory`**, который не вызывается нигде и тянет ранний `plotly.graph_objs` — 1.5 с. **Урок: измерять модуль в одиночку — значит мерить не ту величину; атрибуцию давать профилем импорта, а не отдельным запуском**
[not_included] [Technical] **4/4 положительный контроль — сперва НЕ ОТРАБОТАЛ и едва не был записан как пройденный.** Первая попытка снять seaborn звала `pip`, которого в venv нет (он под uv): вывод ушёл в `tail -1`, пакет остался на месте, а обе функции «успешно» построили фигуры — то есть контроль показал бы зелёный, ничего не проверив. Со второй попытки (`uv pip uninstall`) контроль отработал по-настоящему: `import bquant.visualization` проходит без seaborn, `_seaborn()` отдаёт `None`, обе функции строят фигуры запасной веткой. Это **второй за день случай, когда шаг проверки напечатал ошибку вместо результата** — первый был `codemap search` в гейте
[not_included] [Technical] **Промах в работе:** `git checkout` по одному файлу, сделанный для отката мутации, снёс вместе с мутацией и все правки этого файла. Заметил по тому, что сторож покраснел не тем способом; правки переналожены. Откат мутации делать копией файла, как в остальных случаях сегодня, а не `git checkout`

[included] [other] Гейт перегнан на `8d77a55` уже починенным скриптом: **3576 passed / 34 skipped** на обоих плечах, батарея 36/36, контракт зелёный (`layered (7)`), положительный контроль пересборки отработал **обеими** половинами. Отдельно проверено, что этот контроль умеет краснеть: на графе `3591f83` (до сегодняшних правок) он выдаёт три срабатывания — два «добавленного символа нет» и одно «удалённый символ на месте». Контроль, который не может покраснеть, не проверяет ничего
[included] [other] Потребителю отправлено уточнение в issue №15: сха кандидата сдвинулся с `a5120d3` на `8d77a55`, что добавилось (неломающее), что ломающих правок не прибавилось, и что сам гейт починен — шаг досыпки зависимостей звал отсутствующий `pip` и молча ничего не делал, шаг проверки пересборки звал несуществующую подкоманду и печатал в лог текст ошибки

---

## Политика версий: §7.4 READINESS — «политика версий, читаемая без нас»

Владелец выбрал трэк продукт-реди и отдельно сказал, что готов пересмотреть свои ограничения
(CI, окна устаревания). Первый шаг выбран так, чтобы **не сдвинуть кандидата на релиз**:
проверено, что `git diff 8d77a55..HEAD -- bquant/ pyproject.toml` пуст, а `pyproject.toml`
исключает `tests*`, `docs*`, `devref*` из дистрибутива — значит правки в них колесо не меняют.

[included] [Added] `docs/versioning.md` — политика версий и совместимости для того, кто ставит пакет и не может спросить автора. Восемь разделов: почему `0.0.x`, что здесь считается ломающим изменением (пять форм, из них **три не падают с ошибкой, а меняют числа** — это основная форма в проекте, потому что он чинит дефекты «ответ, похожий на результат»), как узнать что сломалось, как пинить, процедура обновления с пред-регистрацией ожиданий, измеренная каденция, чего проект не обещает, и откуда взяты числа
[included] [Changed] `README.md` — раздел Status переписан: точный пин с примером, прямое предупреждение о том, что опасная поломка не падает, команда `grep` и ссылка на политику. Правила не продублированы: README несёт норму, документ — основания
[included] [Changed] `docs/index.rst` — своя секция «Версии и совместимость», чтобы страница попала на RTD

[not_included] [Technical] **Числа документа — прогоном.** Релизов 16 (`0.0.3` … `0.0.18`), первый 2026-07-24, последний 2026-09-09; по месяцам 1 / 6 / 9. Девять релизов за восемь дней сентября — темп закрытия записей `devref/gaps/`, а не расписание; так и написано

### G81 — обещание changelog'а было верным по существу и непроверяемым на практике

[not_included] [Technical] **Нашлось при написании политики, а не при проверке changelog'а.** Документ должен был выдать читателю одну команду. Оказалось, что выдавать нечего: за шестнадцать релизов сменилось **три** соглашения о пометке ломающих правок — `(ломающее изменение)` в прозе (0.0.5), суффикс `(breaking)` в заголовке раздела (0.0.7–0.0.10), строка `Ломает:` с названной заменой (с 0.0.12) — а **0.0.11 не нёс пометки вовсе**, удалив при этом два публичных поля `SwingThresholds` и сузив ключ метаданных `last_thresholds`
[not_included] [Technical] **Почему это дефект, а не косметика.** Это проектная форма, перенесённая в документацию: читатель, которому обещали, что поломки записаны, грепает, **ничего не находит и делает вывод «поломок нет»**. Пустой результат неотличим от отсутствия события — ровно то же, что `[]` вместо отказа

[included] [Changed] `CHANGELOG.md` — обратно заполнены пометки `Ломает:` там, где их не было: 0.0.5 (удаление `MACDZoneAnalyzer`; переименование `peak_prominence` → `peak_min_amplitude`) и 0.0.11 (удаление двух полей вместе с сузившимся ключом метаданных; подъём `CACHE_VERSION` 20 → 21). **Факты не менялись** — они уже стояли в тех же записях, добавлен токен, по которому их находят
[included] [Changed] `CHANGELOG.md` — две пометки переформатированы (0.0.12, 0.0.14): `Ломает:` стояла в конце строки, содержание переносилось на следующую, и `grep` показывал читателю пометку **без того, что она помечает**. Теперь пометка начинает строку
[included] [Changed] `CHANGELOG.md` — в записи 0.0.5 оговорено, что `docs/migration/MIGRATION_v2.md`, куда она отправляет читателя, удалена в 0.0.13
[included] [Added] `tests/unit/test_changelog_marks_what_it_breaks.py` — 5 правил, 62 проверки: положительный контроль на непустой разбор; релиз с `### Removed` обязан нести пометку; релизы с 0.0.12 не возвращаются к снятым формам; пометка обязана что-то называть; **команда из `docs/versioning.md` обязана находить каждый релиз с удалениями** — регулярка читается из самого документа, поэтому расхождение документа и соглашения краснеет
[included] [Added] `devref/gaps/docs/g81_…md` + строка в реестре

[not_included] [Technical] **Мутации — пять, все красные.** Снять `Ломает:` у 0.0.5 → правило 2. Дописать `### Fixed (breaking)` в 0.0.18 → правило 3. `Ломает: да.` → правило 4. Сломать разбор заголовков (`## [` → `##  [`) → **правило 1**, а остальные три ушли в `skipped`: **61 проверка исчезла**, файл без единого распознанного релиза прошёл бы как безупречный. Заменить регулярку в `docs/versioning.md` → правило 5. Откат каждой мутации — копией файла
[not_included] [Technical] **Сторож нашёл две записи сам, ещё до мутаций** — те самые с переносом строки. Первым уловом сторожа снова оказался не чужой код, а несовершенство собственного соглашения; это третий такой случай за месяц
[not_included] [Technical] **Чего сторож не умеет, названо в записи:** он проверяет наличие пометки, а не её правду. Ломающая правка, которую автор не осознал ломающей и описал в `### Fixed`, пройдёт — в разделе нет `### Removed`, правило 2 молчит. Механической защиты нет; есть гейт и приёмка потребителя

[not_included] [Technical] **Сьют — 3642 passed / 34 skipped** (3676 собрано), дельта от гейта на `8d77a55` (3576 passed) объяснена точно: +62 проверки сторожа changelog'а, +3 блока `docs/versioning.md` у прогонщика примеров, +1 у сторожа молчаливых пропусков, который параметризован по файлам тестов. 66 = 66, необъяснённого остатка нет
[not_included] [Technical] **Кандидат на релиз не сдвинут по содержанию:** правки лежат в `docs/`, `tests/`, `devref/`, `changelogs/`, `CHANGELOG.md`, `README.md` — ничего из этого не входит в дистрибутив (`pyproject.toml`: `exclude = ["tests*", "docs*", "devref*"]`), `bquant/` и `pyproject.toml` не тронуты. Колесо, которое соберётся, тождественно тому, про которое лаба принимает решение

---

## Два решения владельца пересмотрены: CI и окна устаревания

Владелец 2026-09-12 сказал, что готов пересмотреть §3.1 и §3.2 разбора готовности. По обоим
выбрана **не общепринятая форма**, и причины выписаны в решении, а не в коммите.

[included] [Added] `devref/architecture/ci_and_api_windows_decision_2026-09-12.md` — живая запись решения: что заменило прежние «нет», что рассмотрено и отвергнуто, приёмка отложенной части. Снимок `READINESS_2026-09-10.md` не правился: он заморожен и законно записывает то, что было верно на его дату

[not_included] [Technical] **Окна устаревания — отвергнуты в пользу отказа с названной заменой.** Два возражения против полных окон, и второе сильнее: алиас, молча перенаправляющий, есть ровно та форма дефекта, которую проект в себе чинит; но главное — **окно защищает только от переименований, то есть от громкой безопасной половины поломок.** Из пяти форм ломающих изменений три не поднимают исключения вовсе (сменилось умолчание, исправлен расчёт, поднят `CACHE_VERSION`) — от них окно не спасает никак. Принято: реестр удалённых имён + модульный `__getattr__`, обращение падает, но сообщение называет версию удаления и замену. Не смягчение отказа, а доведение правила «отказывать громко и называть причину»
[not_included] [Technical] **Отложено намеренно:** трогает `bquant/`, а наружу выдан кандидат `8d77a55`. Приёмка выписана заранее, пять пунктов, включая положительный контроль (живое имя через тот же `__getattr__` обязано разрешаться — иначе «реестр работает» неотличимо от «модуль сломан целиком»)

[not_included] [Technical] **CI — отвергнута матрица на каждый пуш.** Она дублирует гейт, добавляет задержку при одном разработчике и **дрейф зависимостей ловит не лучше** недельного прогона: дрейф происходит на стороне PyPI и к темпу наших коммитов не привязан. Условие пересмотра из §3.1 («внешние потребители помимо лабы») не наступило — пересмотр по решению владельца, и так и записано

[included] [Added] `.github/workflows/weekly-fresh-resolution.yml` — недельный прогон плюс ручной запуск: установка **без лок-файла**, два плеча `pandas` (3.x и 2.x), Python **3.12** — нижняя граница `requires-python`, которую не покрывает ни одна локальная машина (там 3.13). Закрывает единственную дыру гейта: «библиотека выпустила версию, на которой мы краснеем» было видно **в день релиза** (так найден G50), теперь окно семь дней. Условием релиза не является
[included] [Changed] `docs/versioning.md` §7 — заявление про CI переписано: прогонов на пуш нет и не обещается, недельный прогон есть с 2026-09-12, и названо, чего он не делает

[not_included] [Technical] **В прогоне есть шаг «положительный контроль», и его форма взята из сегодняшних же провалов гейта.** Задание валится, если: установленная `pandas` не того мажора, которым названо плечо (иначе два задания проверяют одно и то же, а выглядят как два); `hypothesis` отсутствует (сторож инвариантов импортирует его на верхнем уровне — это ошибка сбора, не пропуск); сбор вернул ненулевой код; собрано меньше 3600 тестов (на 2026-09-12 — 3677). Отдельным шагом печатается состав разрешения: зелёный прогон без него сообщает «что-то прошло», а не «на чём было зелено»
[not_included] [Technical] **Локальный прогон контроля нашёл в нём настоящую ошибку разбора.** Номер в строке `====== 3676 tests collected ======` стоит **вторым** токеном; мой разбор брал первый и делал `int('')` — шаг падал бы на каждом прогоне с трассировкой вместо вердикта. Поймано тем, что контроль был извлечён из yaml и запущен, а не прочитан глазами. Заменено регуляркой, добавлена проверка кода возврата сбора
[not_included] [Technical] **Мутации контроля — четыре, все красные:** плечо названо `3` при установленной pandas 2 → «плечо 3, а установлена pandas 2.3.3»; порог 99999 → «собрано 3677, порог 99999»; имя `hypothesis` подменено на отсутствующее → «инварианты не проверятся»; каталог тестов подменён → «сбор вернул код 4» **и** «собрано 0». Проверялась не копия по памяти, а текст, извлечённый из самого workflow
[not_included] [Technical] Сьют собирает **3677** (было 3676): +1 — проверка ссылки, которую `test_docs_parity` параметризует по каждой markdown-ссылке, а в политику добавлена ссылка на файл workflow

### Побочная находка: `full` обещает больше, чем содержит

[not_included] [Technical] README: «`full` (= `dev` + `notebooks`)». Фактически `full` — пять пакетов, `dev` + `notebooks` — шесть: **`hypothesis` в `full` не попал**, когда его добавили в `dev` вместе с проверкой инвариантов. Последствие громкое, не тихое (импорт на верхнем уровне → ошибка сбора), но заявление README неверно. Класс тот же, что G33: два списка, обязанные совпадать, разъехались при правке одного. Существующий сторож `test_contracts_are_checked.py` проверяет, что экстры существуют и не дублируют базу, — равенства `full = dev ∪ notebooks` он не проверяет
[not_included] [Technical] **Отложено:** правка `pyproject.toml` меняет метаданные колеса, то есть сдвигает выданного кандидата. Идёт в релизную подготовку (она `pyproject.toml` трогает всё равно — номер версии) вместе со сторожем на равенство, и попадает в сообщение лабе как неломающее изменение состава `full`

### Недельный прогон нашёл дефект первым же запуском — G82

[not_included] [Technical] **Плечо pandas-2: `Fatal Python error: Aborted`, core dumped, rc 134** — внутри `pandas_ta.py:433 calculate`, зван из `zigzag.py:104 _swing_context` под широким поиском инвариантов. Плечо pandas-3 прошло полностью. Перезапуск того же задания прошёл зелёным. Разрешения обоих плеч совпадают во всём, кроме pandas (3.0.5 / 2.3.3): `pandas-ta` 0.4.71b0, `numpy` 2.2.6, `scipy` 1.18.1, `hypothesis` 6.168.0 — значит дело во **входе**, не в версии библиотеки
[not_included] [Technical] **Два факта, и смешивать нельзя.** (а) `pandas-ta` уводит интерпретатор в `abort()` изнутри скомпилированного кода на каком-то допустимом кадре — наш `try/except Exception` вокруг `zigzag.calculate` такого не ловит **в принципе**, и аккуратно написанный отказ «ZigZag global pass could not run» в этом режиме недостижим. (б) Наш широкий поиск **терял находку**: `derandomize=False` (намеренно — он затем и широкий), и при жёсткой смерти контрпример гибнет вместе с процессом, `hypothesis` не успевает ни сжать, ни напечатать. Осталась трассировка без входа
[not_included] [Technical] **Докстрока файла утверждала неверное:** «seed фиксирован, поэтому красный тест воспроизводится». Для бюджета по умолчанию (`derandomize=True`) правда; **для широкого поиска — нет**, а он и находит редкое. Утверждение было верным ровно про тот режим, который нового не пробует

[included] [Added] `tests/unit/test_invariants_hold_on_generated_data.py` — `_record_wide_example()`: пробуемый кадр выгружается **до** проверки. В файл, а не в `print` (захваченный вывод при `abort()` теряется вместе с процессом); `flush` + `fsync` (буфер теряется тоже); перезапись на каждом примере — нужен последний, тот, на котором умерли; под переменной `BQUANT_WIDE_EXAMPLE_DUMP`, чтобы локальный прогон не сорил в дереве
[included] [Changed] `.github/workflows/weekly-fresh-resolution.yml` — переменная выгрузки на шаге сьюта и подъём файла артефактом `wide-example-<плечо>` шагом `if: failure()`
[included] [Changed] докстрока файла инвариантов — режимы разведены: у бюджета seed фиксирован, у широкого поиска нет, и поэтому он выгружает кадр
[included] [Added] `devref/gaps/validation/g82_…md` + строка в реестре

[not_included] [Technical] **Механизм проверен в обе стороны:** без переменной файла не появляется; с переменной появляется; файл **читается обратно** как валидный кадр — `(60, 5)`, колонки `open/high/low/close/volume`, индекс возрастающий. Обратное чтение проверялось намеренно: файл, который есть и не читается, — та же пустота, только с именем
[not_included] [Technical] **Что эта правка НЕ даёт, названо в записи:** она не делает пакет устойчивее. Она делает следующее падение пригодным для разбора. Аборт `pandas-ta` остаётся открытым — без входа сообщать вверх по течению нечего
[not_included] [Technical] **Это ровно та ценность, ради которой прогон и заведён, и она пришла на первом запуске.** Дефект нашёлся не потому, что кто-то догадался его искать, а потому, что поиск шёл по формам, которых прежние прогоны не пробовали. Гейт этого не нашёл ни на одном из девяти сентябрьских релизов

[included] [other] Потребителю сообщено про аборт `pandas-ta` (issue №15, отдельная запись): наблюдение, **не регрессия** — код этого пути в кандидате тождествен 0.0.18, который они уже гоняют. Сказано прямо: воспроизводимого входа нет и почему (недетерминированный поиск плюс жёсткая смерть), что `except Exception` такого не ловит в принципе, что вход был сгенерированный, а не реальные данные, и что тихого искажения чисел эта форма не даёт — процесс умирает, а не возвращает неверный результат. Отдельно повторено, что сха кандидата по содержанию не сдвинут: правки после `8d77a55` лежат вне дистрибутива
