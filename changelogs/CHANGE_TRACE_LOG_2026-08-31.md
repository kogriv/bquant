# Трейслог изменений — 2026-08-31

[Волна 2 прохода по докам: основной путь пользователя, и три дефекта кода под ним]

[not_included] [Technical] Волна 1 закоммичена отдельно (`706f777`) после прогона, прерванного накануне: 2956 passed, 36 skipped. Греп диффа по правилам публичного репозитория чист. Сборка сайта пересчитана — 23 предупреждения против 30 до волны 1; вчерашняя цифра 24 была цифрой холодной сборки
[not_included] [Technical] Волна 2 — шесть страниц основного пути: `zone_analysis.md`, `zone_analysis_result.md`, `api/analysis/pipeline.md`, `swing_strategies.md`, `caching.md`, `best_practices.md`. Правило прохода то же: числа получать прогоном. Оно и сработало — **из шести страниц пять содержали неисполнимый или неверный код**, и три дефекта оказались не в тексте, а в пакете
[included] [Fixed] docs/user_guide/core_concepts.md — **собственная ошибка волны 1, найденная прогоном волны 2:** страница печатала `# 32` там, где ответ 83. Пресет `analyze_macd_zones` детектирует по знаку **линии** MACD (`zone_basis='line'`), а пример рядом — по гистограмме; гистограмма меняет знак чаще, зон получается втрое больше. Слой поведения этого не ловит: он исполняет пример, но не сверяет напечатанное с комментарием. Разница выведена в текст обеих страниц как содержательная — выбор основы зоны есть выбор вопроса
[included] [Changed] docs/user_guide/zone_analysis.md — переписана (2219 → 1344 слова). Ни один пример не исполнялся: все начинались с `price_df`/`df`, которых нигде нет, поэтому слой поведения страницу не брал вовсе. **Два примера были неверны:** модульная детекция с `rules={'indicator_role': 'hist'}` без пайплайна даёт `ValueError` (роль разрешает схема, которую строит пайплайн), а `conditions` для стратегии `combined` описаны как словари `{'type': 'lambda', 'func': ...}` — на деле это список функций, словарь даёт `'dict' object is not callable`
[included] [Removed] docs/user_guide/zone_analysis.md — две ASCII-простыни архитектуры, псевдоисходники `ZeroCrossingDetection.detect_zones` и `UniversalZoneAnalyzer.analyze_zones` (второй литерал реализации, обязанный совпадать с кодом и никем не сверяемый), разделы «Уникальные особенности v2.1» и «Главные преимущества», ссылка на миграцию с класса, удалённого в 0.0.5
[included] [Fixed] docs/user_guide/zone_analysis.md — таблица сравнения режимов свингов содержала «coverage 70–90%» против «18–62%» без указания, на чём мерено. Ни одна из четырёх цифр не воспроизводится: заменена измеренной матрицей 3 стратегии × 2 пресета × 2 режима
[included] [Added] devref/gaps/swing/g35_default_preset_measures_nothing_and_says_nothing_2026-08.md — **G35, найден этим замером.** `min_amplitude_pct` пресета `default` — 2% цены; медианный размах зоны на встроенном сэмпле — 1.2%. Мерка крупнее измеряемого, и свинг внутри зоны пройти её не может: `find_peaks` даёт 0/32 зон, `pivot_points` 0–2/32, тогда как `narrow_zone` — 20/32 и 23/32. Ноль при этом неотличим от честно измеренного отсутствия движения. Картина не новая: отчёт `swing_strategy_comparison_case_study.md` зафиксировал те же нули в ноябре 2025 и вывел «берите zigzag» — вывод сделали, умолчание оставили, пользователю в месте выбора не сказали
[included] [Changed] docs/user_guide/zone_analysis_result.md — страница-справочник, содержательно верная, но **не исполнявшаяся ни в одной строке** (2633 → 2970 слов: прибавка целиком на самодостаточность примеров). Исправлен неработавший модульный пример; добавлено поле `column_schema`, которого в описании результата не было вовсе; тип `zone_id` в признаках указан неверно (`str/int` вместо `int`); из `summary.json` убрана ловушка `bull_zones_count`/`bear_zones_count` — на не-MACD осцилляторе она пишет нули в отчёт (G34)
[included] [Added] devref/gaps/cache/g36_the_cache_key_does_not_see_the_strategies_2026-08.md — **G36, самый серьёзный из прохода: результат неверен и молчит.** Пример со всеми пятью стратегиями метрик дал `volatility_metrics: None` у всех зон, в логе — `loaded from cache`. Замер: 29/32 зон с метриками при выключенном кэше против **0/32** при включённом. Причина в `pipeline.py:470` — ключ собирается из хэша данных, подписи конфигурации и подписи свингов, а `shape`, `divergence`, `volatility` и `volume` приходят мимо конфигурации, через `zone_analyzer=`. Ключи двух разных конфигураций совпадают побайтно (проверено прямым сравнением). Кэш включён по умолчанию, дисковый живёт час
[included] [Added] devref/gaps/metrics/g37_nan_fails_a_non_negativity_assert_2026-08.md — **G37.** У зоны ровно в окно Боллинджера (20 баров) ширина считается один раз, `std` по одному значению — `NaN`, а `NaN >= 0` ложно. `assert` роняет всю группу метрик волатильности и объясняет это неверными словами: величина не отрицательна, её просто нет. В том же файле **39 таких `assert`** — и все исчезнут под `python -O`
[included] [Changed] docs/api/analysis/pipeline.md — переписана (1454 → 1152 слова). Рекламировала **четыре несуществующие стратегии** — `shape='geometric'`, `divergence='hidden'`, `volume='correlation'`, `volatility='statistical'` — причём дважды, в двух разных списках. Список заменён полученным из `StrategyRegistry` прогоном. «115 тестов, 100% pass rate» — тоже дважды. Половина примеров лежала в ```text-блоках, то есть была заведомо непроверяемой. Добавлены `with_swing_preset`, `with_auto_swing_thresholds`, `min_duration` — методов и параметра в справочнике билдера не было
[included] [Changed] docs/user_guide/swing_strategies.md — примеры сделаны самодостаточными, убран лишний `set_index("time")` (пайплайн переносит время сам — G30), добавлена измеренная матрица покрытия по пресетам. `with_auto_swing_thresholds` описан по замеру: меняет `deviation` ZigZag с 0.05 на 0.031, но покрытия на этих данных не прибавляет — «адаптивный» в названии не обещает результата
[included] [Fixed] docs/user_guide/caching.md — утверждала: «при любом изменении (… пресет, **стратегия**) генерируется другой ключ». Это и есть G36, только со стороны доки: обещание, которого кэш не выполняет. `CACHE_VERSION` названа равной **2** при фактических **15** — номер переписали в доку один раз, и он отстал на тринадцать; теперь страница показывает, как спросить его у кода. Вывод `cache_stats()` описан неточно. Про lambda в правилах сказано «кэш может не работать» — на деле не работает никогда и по конкретной причине: в ключ попадает `repr` функции с адресом в памяти
[included] [Changed] docs/user_guide/best_practices.md — переписана. Все примеры были псевдокодом с **несуществующими функциями** (`save`, `load`, `extract_features`, `analyze_statistics`, `full_analysis`) — форма, которая читается как API и им не является; это ровно «хорошо оформленная ложь», против которой в проекте написано четыре слоя проверок. Заменены тремя исполняемыми паттернами. Убрана ссылка в `devref/` — она уводила читателя сайта за пределы дерева доков
[included] [Fixed] docs/developer_guide/zone_analyzer_deep_dive.md — упоминался пресет `'wide_zone'`, которого нет: в `SWING_PRESETS` два имени, `default` и `narrow_zone`. Точечная правка, полный проход страницы — волна 5
[included] [Fixed] docs/tutorials/macd_basic_pipeline.md — ссылка на якорь `#пример-1-macd-analysis`, исчезнувший при переписывании `pipeline.md`. Поймано сборкой сайта: **пятый слой проверок работает ровно на том, чего не видят четыре первых** — ссылка на раздел внутри страницы
[included] [Changed] devref/gaps/gap_inventory_2026-07.md — раздел «Открыто» перестал быть пустым: G34, G35, G36, G37, все четыре найдены проходом по докам и все — попыткой честно описать, что код делает на самом деле
[included] [Changed] devref/architecture/docs_sequential_pass_2026-08.md — волна 2 закрыта, по каждой странице записано, что нашлось
[included] [Fixed] .gitignore — шаблон `cache/` без ведущего слэша ловил **любой** каталог с таким именем на любой глубине, и документ гэпа G36 (`devref/gaps/cache/`) молча не попадал в коммит. Поймано перед коммитом сверкой `git status` со списком созданных файлов, а не догадкой. Заменён на `/cache/` — проверено, что во всём дереве под прежний шаблон подпадал только этот каталог
[not_included] [Technical] Проверка волны 2: полный сьют **2986 passed, 36 skipped, 0 failed**. Слои доков 1725 проверок против 1689 до прохода. Сборка сайта 21 предупреждение против 30 до волны 1 — и одно из них появилось по моей вине (ссылка на якорь, исчезнувший при переписывании `pipeline.md`), было поймано сборкой и починено
[not_included] [Technical] Побочное наблюдение, требующее внимания: сьют замедлился с 4 мин 11 с до **13 мин 39 с**. Причина — самодостаточные примеры волны 2: каждый строит полный пайплайн, часть с выключенным кэшем (иначе они бы врали, см. G36). Плата за исполняемую доку реальна и её стоит замерить отдельно

[Кэш перестал отвечать на другой вопрос, а метрика — выдавать отсутствие за ноль]

[not_included] [Technical] Порядок выбран не по тяжести, хотя по ней вышло бы так же. G36 **въедался в доку**: предупреждение «сравниваете стратегии — выключайте кэш» пришлось написать на трёх страницах волны 2, и на каждой следующей странице про стратегии его пришлось бы писать снова. Дока начала обслуживать дефект вместо того, чтобы описывать пакет — это и был сигнал чинить сейчас, а не после волны 5
[included] [Fixed] bquant/analysis/zones/{pipeline,cache}.py — **G36 закрыт.** Ключ кэша получил четвёртую часть: подпись анализатора. `_serialize_analyzer_configuration()` описывает пять семейств метрик (класс плюс параметры — через `config_hash()`, если есть, иначе через `get_metadata()['params']`) и классы внедрённых компонентов; `ZoneAnalysisCache.analyzer_signature()` хеширует это наравне с данными, конфигурацией и свингами. Свинги остались в своей подписи и продублированы в новой: цена — одна строка, альтернатива — правило, которое держится на памяти
[included] [Removed] bquant/analysis/zones/pipeline.py — **второй литерал номера схемы кэша.** `CACHE_SCHEMA_VERSION = 15` в пайплайне и `ZoneAnalysisCache.CACHE_VERSION = 15` в кэше были двумя числами на одном значении, а правило «бампятся вместе» жило только в прозе четырёх гэп-документов (G15, G16, G20, G30) — тот же дефект, что закрывали в G33. Теперь пайплайн берёт номер у кэша. Доказательство ненадёжности правила лежало рядом: комментарий с историей версий в пайплайне отстал на версию от самого числа, который он объясняет. Сама история версий не потеряна — она и раньше велась в двух местах, осталась в `cache.py`, где номер и живёт
[included] [Changed] bquant/analysis/zones/cache.py — `CACHE_VERSION` 15 → 16: записи прошлых версий отвечают на вопрос «этот индикатор, эта детекция», не различая, какие метрики просили посчитать
[included] [Added] tests/unit/test_cache_key_sees_the_analysis.py — включённая стратегия обязана менять ключ (параметризовано по семействам); другие параметры той же стратегии — тоже; та же конфигурация обязана давать тот же ключ (иначе кэш просто не работал бы); подменённый компонент меняет ключ. Отдельно — что номер схемы **один**, и проверяется это разбором AST: два литерала `15` прошли бы и `==`, и `is`, потому что маленькие целые в CPython — один и тот же объект. Тот случай, когда равенство значений ничего не доказывает
[included] [Added] tests/integration/test_cache_serves_what_was_asked.py — сценарий потребителя, а не ключа: прогон без стратегии наполняет кэш, следующий с включённой стратегией обязан получить метрики. Именно этот путь и был сломан
[not_included] [Technical] Мутационная проверка обеих правок: на коде до фикса (`git stash push bquant/`) краснеют **7 из 10** новых проверок кэша, включая сценарий потребителя, и **6 из 6** проверок метрик. Три зелёные в кэшевом наборе — те, что стерегут отсутствие лишних промахов, они и обязаны быть нечувствительны
[included] [Fixed] bquant/analysis/zones/strategies/base.py, strategies/volatility/combined.py — **G37 закрыт.** Отсутствие выражается `None`, а не нулём: `bollinger_width_pct`, `bollinger_width_std`, `bollinger_squeeze_ratio` и счётчики касаний стали `Optional`; разброс считается только при двух и более наполненных окнах — `std` по одному значению не «нулевой разброс», а отсутствие величины
[included] [Fixed] bquant/analysis/zones/strategies/volatility/combined.py — ветка `except` возвращала полный набор нулей со `squeeze_ratio: 1.0`, то есть правдоподобное измерение вместо признания, что посчитать не удалось. На встроенном сэмпле так «измерялись» **13 зон из 32**. Теперь недостающее отсутствует, а ATR-часть остаётся измеренной: у зоны в 4 бара `bollinger_width_pct: None` и `avg_atr: 5.46` — читатель видит, что именно известно
[included] [Changed] bquant/analysis/zones/strategies/volatility/combined.py — `volatility_score` и `volatility_regime` стали `Optional` и отсутствуют вместе: композит по шкале 0–10 на две трети складывается из боллинджеровских компонент, и число по той же шкале без них несопоставимо с другими зонами. Проверка пинит это в обе стороны — счёт без режима и режим без счёта одинаково запрещены
[included] [Changed] bquant/analysis/zones/strategies/base.py — **39 `assert` заменены на `_require`** с `AnalysisError`. У `assert` здесь два негодных свойства: он исчезает под `python -O` вместе с проверкой (и тогда `NaN` уезжает в статистику молча) и объявляет ошибкой программиста то, что является свойством данных. Введён `_is_defined()`: `None` — законное «не измерено» и проходит проверки, `NaN` остаётся ошибкой, но названной своим именем — «величина не измерена, а не отрицательна»
[included] [Fixed] bquant/analysis/zones/zone_features.py — **метрики терялись ещё в одном месте, и это место — сообщение о них.** Строка отладочного лога форматировала `volatility_score:.2f`; на `None` она падала с `TypeError`, падение ловил окружающий `except`, и посчитанные метрики выбрасывались. Форматирование переведено на ленивое `%s`. Замер после всех правок: группа метрик есть у **30 зон из 32** (было 29, из которых 13 несли выдуманные нули); без группы остались зоны в 1 и 2 бара, где стратегия честно отказывается по минимальной длине
[included] [Added] tests/unit/test_an_absent_metric_is_not_a_zero.py — шесть утверждений: зона длиной ровно в окно сохраняет метрики с `None` вместо `NaN`; отсутствие полос не притворяется нулём, а ATR остаётся; композит отсутствует вместе с компонентами; `NaN` отвергается сообщением, называющим его неизмеренностью; счёт и режим отсутствуют вместе; в модуле не осталось `assert` — проверяется по AST, потому что под обычным прогоном разницы между `assert` и `raise` не видно, а под `-O` видно слишком поздно
[included] [Changed] docs/{api/analysis/pipeline,user_guide/caching,user_guide/zone_analysis_result,user_guide/best_practices}.md — предупреждения об обходе сняты: сравнивать стратегии теперь можно с включённым кэшем. Факт оставлен как история версии схемы, а не как совет. В описании `volatility_metrics` сказано главное: `None` там означает «не измерено», и вместе с `volatility_score` отсутствует `volatility_regime`
[included] [Changed] devref/gaps/cache/g36_…md, devref/gaps/metrics/g37_…md, devref/gaps/gap_inventory_2026-07.md — оба гэпа закрыты с разбором починки и ценой: первый прогон после обновления пересчитает то, что раньше приходило из кэша
[included] [Fixed] examples/05_strategies_demo.py, docs/api/visualization/README.md — тот же дефект снаружи пакета, найден при проверке потребителей: оба читали `volatility_score` с **верхнего уровня** `features`, где его нет (метрики стратегий лежат в `features['metadata']`), и оба через `.get(..., 0)`. То есть пример годами печатал `Score: 0.0/10`, а страница визуализации строила box-plot из нулей по умолчанию — и ни один прогон об этом не сообщал, потому что ноль исполняется так же хорошо, как число. Теперь читают из `metadata`, а отсутствие называют словами
[not_included] [Technical] Итог прогона после починок: **3005 passed, 36 skipped, 0 failed** — и сьют ускорился с 13 мин 39 с до **5 мин 43 с**. Причина в самой правке G36: примеры доки с одинаковой конфигурацией снова попадают в кэш, а из `zone_analysis_result.md` ушёл вынужденный `with_cache(enable=False)`. Дефект кэша стоил не только верности результата, но и трети времени сьюта

[Ноль научился говорить о себе, а сводка — говорить на языке своего индикатора]

[not_included] [Technical] Три решения владельца, принятые по замерам: G35 — громкий ноль сейчас, смена умолчания потом; G34 — вариант (c), `zones_by_type` плюс направленные поля только для своего словаря; порядок — волна 3, релиз потом
[included] [Added] bquant/analysis/zones/analyzer.py — **G35, первая половина.** `result.metadata['swing_coverage']`: сколько зон получили хотя бы один свинг, какой стратегией, из скольких. Плюс предупреждение в лог, когда улов пуст **у всех** зон. Два ответа вместо одного намеренно: число — чтобы могла спросить программа, предупреждение — чтобы человек не искал причину в рынке. Текст называет и причину, и что попробовать (`narrow_zone`), и где разбор
[included] [Added] tests/unit/test_a_swing_strategy_that_finds_nothing_says_so.py — в том числе обратная сторона: при непустом улове предупреждения быть **не должно**, иначе оно превратится в фон и перестанет читаться
[not_included] [Technical] По дороге вскрылась особенность самого пакета, о которой стоит помнить при любых проверках логов: логгеры настроены с `propagate=False`, поэтому обычный `caplog` их не видит — сообщение печатается на экран, а проверка молчит и тест зеленеет ни на чём. Обработчик приходится вешать на конкретный логгер. Первая версия теста прошла бы именно так
[included] [Fixed] bquant/analysis/zones/zone_features.py — **G34 закрыт вариантом (c).** `total_statistics` всегда отдаёт `zones_by_type` и `ratios_by_type` по фактически встреченным типам; `bull_zones_count`/`bear_zones_count`/`bull_ratio`/`bear_ratio` остались только там, где эти типы есть. MACD не изменился (16/16, 0.5/0.5), у RSI направленных полей больше нет вовсе вместо четырёх нулей при 64 зонах
[included] [Changed] bquant/cli.py — `SUMMARY_SCHEMA_VERSION` 1 → 2: `total_statistics` уходит в `--json` как есть, значит смена его формы обязана сдвинуть номер. Проверено обоими путями CLI: MACD печатает и распределение, и направленные поля, RSI — только распределение
[included] [Changed] bquant/analysis/zones/cache.py — `CACHE_VERSION` 17 → 18 (форма статистики) и 16 → 17 перед этим (`swing_coverage` в метаданных). Обе записи прошлых версий отвечали бы на вопрос иначе, чем спрашивают сейчас
[included] [Added] tests/unit/test_the_aggregate_speaks_the_indicator_vocabulary.py — проверка **двусторонняя намеренно**: односторонняя («на RSI нет нулей») разрешила бы заодно выбросить `bull_*` у MACD и сломать внешнего потребителя. Отдельно пинится, что номер схемы CLI сдвинулся вместе с формой
[included] [Fixed] examples/04_comprehensive_analysis.py — печатал `Bull ratio` / `Bear ratio` через `.get(..., 0)`; на пороговом осцилляторе это два нуля. Теперь печатает распределение по фактическим типам
[included] [Changed] docs/user_guide/{quick_start,cli,zone_analysis_result,best_practices}.md — предупреждения о расхождении сняты, вместо них описана новая форма. Осталось ровно одно указание: программе, читающей сводку, проверять надо **наличие** ключа, а не его значение
[not_included] [Technical] Мутационная проверка: на коде до правок краснеют 3 из 4 проверок G34 и 3 из 4 проверок G35. Зелёные — те, что стерегут отсутствие лишнего шума и лишних промахов; они и обязаны быть нечувствительны
[not_included] [Technical] **Условие релиза, а не правки:** G34 меняет форму, объявленную в схеме CLI, и вторая половина G35 (смена умолчания пресета) изменит свинг-метрики всем. Перед выпуском нужен issue внешнему потребителю и его прогон на своих данных
[not_included] [Technical] Заведено issue внешнему потребителю в его трекере — условие выпуска 0.0.10. В нём: что изменилось (G34/G35/G36/G37), что уже проверено по его чекауту чтением (пресет свингов он задаёт явно, из пяти семейств метрик включает только свинги, `total_statistics` нигде не читает — то есть ни одна правка его, судя по всему, не задевает), и что прошу подтвердить прогоном: отпечаток эталонных зон в его собственной граничной проверке и отсутствие чтения агрегата вне его репозитория. Отдельным вопросом — не помешает ли ему будущая смена умолчания пресета
[not_included] [Technical] **Найдено при подготовке issue, требует отдельного решения:** имя приватного соседнего проекта встречается в этом публичном репозитории **18 раз в 7 файлах** (`changelogs/CHANGE_TRACE_LOG_2026-07-20.md`, `_2026-07-24.md`, `devref/architecture/{package_charter,revival_plan_2026-07}.md`, `devref/cleanup/{oss_readiness_plan_2026-07,package_drying_inventory_2026-07-20,docs_structure_audit_2026-07-22}.md`), в 12 коммитах истории. AGENTS.md это прямо запрещает. Записи выше и мои прошлые придерживаются формулировки «внешний потребитель», но прежние — нет. Правка `HEAD` не убирает имя из уже отправленной истории, поэтому это отдельная задача с отдельным решением владельца, а не строка в этом коммите
