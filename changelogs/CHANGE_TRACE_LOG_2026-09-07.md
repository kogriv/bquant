# Change Trace Log — 2026-09-07

[Сторож повторных определений: дыра, о которой сообщила лаба (bquant#118)]

[included] [Fixed] tests/unit/test_no_shadowed_definitions.py — исключение по декоратору снимало подозрение с группы **целиком**: имя под `@property` не попадало в `seen`, и обычное определение **после** группы встречало пустой след. Замерено исполнением, а не выведено из чтения: после второго `def x` в `C.__dict__['x']` уже не `property`, а `c.x` — связанный метод вместо значения, то есть меняется тип доступа. Это ровно та форма, ради которой сторож заводился
[included] [Changed] tests/unit/test_no_shadowed_definitions.py — маркеры разведены на два семейства, потому что после группы они ведут себя противоположно: `property`/`setter`/`getter`/`deleter`/`cached_property` — обычное определение после них затирает; `overload`/`register`/`singledispatch*` — обычное определение после них **реализация**, законное завершение шаблона. Правило переписано на **предыдущее** связывание: находка, если предыдущее было обычным (его убивает любое следующее) либо предыдущее аксессор, а текущее обычное; предыдущая перегрузка не убивает ничего
[included] [Added] tests/unit/test_no_shadowed_definitions.py — три положительных контроля: обычное определение после `@property` (с исполнением: `isinstance(..., property)` → False, `c.x` вызываем), реализация после `@overload` (с исполнением: `D().f(5) == 5`, `D().f('s') == 's'`), обычное определение **перед** `@property`
[not_included] [Technical] Мутации: вернуть старую ветку «декоратор снимает группу» — 2 красных (после `@property` и перед ним); «любой повтор — находка» — 2 красных (группа аксессоров и реализация после `@overload`). Дыра пустая: по 311 отслеживаемым `.py` исправленный сторож даёт 0 находок, как и до правки
[not_included] [other] Дефект нашла лаба, забрав сторож к себе и дописав к пункту «исключение по декоратору» положительный контроль. Зонд, которому ни разу не показали то, что он обязан найти, — не зонд; у нас этот пункт контроля не имел

[G48: меняет ли правка неадаптивный путь — замер, а не рассуждение]

[not_included] [Technical] Вопрос от лабы к уведомлению о будущем релизе: меняет ли `fbfa9ff` вывод на неадаптивном пути (`zigzag` для глобальных свингов + `zero_crossing` для зон). Замер: рабочее дерево и рабочая копия на `f324f11` (родитель `fbfa9ff`), один и тот же зонд, оба встроенных сэмпла (`tv_xauusd_1h` — 77 зон, `mt_xauusd_m15` — 91), кэш выключен, дамп всех признаков каждой зоны в канонический JSON. Неадаптивный `zigzag`: `global` и `per_zone` — **побайтно одинаково** (md5 совпадает). Тот же зонд как положительный контроль на адаптивном пути: `find_peaks` — различие, `pivot_points` — различие, `zigzag` — совпадение. То есть зонд различия видит, и на неадаптивном пути их нет
[included] [Changed] CHANGELOG.md — блок G48 в `[Unreleased]` называет этот замер: неадаптивный путь не меняется, и это замерено

[not_included] [other] Лаба уведомлена досрочно: kogriv/bquearch#11 — ответ по G48 (правка в `main`, не отгружена; неадаптивный путь не меняется, замер и положительный контроль) и закрытие bquant#118. Релиза под G48 пока нет: один блок в `[Unreleased]`, копится к следующему

[Циклы в контракте: правило включено, вторая правда о ключе кэша удалена (codmap 0.0.14)]

[not_included] [Technical] codmap 0.0.14 разделил виды рёбер: у нас **0 eager / 9 lazy / 1 type-only**. Единственное type-only ребро — `zones/cache.py → pipeline` под `if TYPE_CHECKING:`, ради аннотации в `config_signature`; из-за него `no_cycles` стоял выключенным с 2026-09-06 (codemap#18)
[included] [Removed] bquant/analysis/zones/cache.py — `ZoneAnalysisCache.config_signature()` **не вызывался ниоткуда** (греп по пакету и тестам: живой путь — `self.config.to_cache_key()` в пайплайне) и уже разошёлся с живым: сериализовал семь полей, без `min_duration` и `atr_period`, которые G60 добавил в ключ. Пока метод мёртв — это вторая правда о ключе; вызови его кто-нибудь, и два прогона с разным периодом ATR делили бы одну запись. Вместе с методом ушли `TYPE_CHECKING`-импорт и `asdict`; строка `Args:` теперь называет живого производителя ключа
[included] [Changed] codemap.toml — `no_cycles = true`, `no_type_only_cycles = true`; комментарии называют, почему правило было выключено и что его открыло. `no_lazy_cycles` остаётся `false`: `indicators.base` ↔ `custom.*` замыкаются через фабрику намеренно
[included] [Added] tests/unit/test_layers_point_one_way.py — те же два правила без инструмента: граф импортов строится по тексту пакета, рёбра различаются на три вида (верхний уровень / тело функции / под `TYPE_CHECKING`), два теста требуют ацикличности eager-графа и графа «eager + type-only». Сообщение называет цикл целиком (`models -> pipeline -> analyzer -> models`). Третий тест пинит сам классификатор: без него «циклов нет» означало бы только, что сканер не видит рёбер
[not_included] [Technical] Мутации: вернуть `TYPE_CHECKING`-импорт в `cache.py` — красный только type-only тест, eager зелёный (виды рёбер действительно различаются); настоящий eager-цикл (`models` импортирует `pipeline` на верхнем уровне) — красные оба. **Первая попытка второй мутации не применилась**: вставка «после первого `\n\n`» попала внутрь докстринга, тесты остались зелёными. Зелёная мутация — сначала повод проверить, что мутация вообще применилась, и только потом вывод о стороже
[included] [Changed] CHANGELOG.md `[Unreleased]` — «Removed» и «Changed»; AGENTS.md — раздел «Layers point one way» называет ацикличность и три вида рёбер
[not_included] [Technical] Гейт: чистый клон, зависимости как разрешаются сегодня, оба плеча — pandas 3.0.5 **3470 passed, 34 skipped, 0 failed** и pandas 2.3.3 **3470 passed, 34 skipped, 0 failed** (+7 к 3463: 3 сторожа циклов, 4 добора); батарея 36 из 36 с кодом 0; `codemap check --require-contract` на 0.0.14 — rc 0, «Contract satisfied. Rules enforced: layered (6), forbidden (8), no_cycles, exhaustive», в сноске «0 import(s) under `TYPE_CHECKING`»
[not_included] [Technical] Положительный контроль на сам гейт: `no_type_only_cycles` в списке enforced не назван (называть нечего — рёбер этого вида на дереве не осталось), и «зелено» могло означать «ключ не распознан». Проверено пробой на клоне: вернули `TYPE_CHECKING`-импорт и аннотацию — `❌ 1 rule(s) broken. no_type_only_cycles — 1 dependency cycle(s) closed only by an import under `if TYPE_CHECKING:`` с полным путём цикла. Правило подключено, а не проглочено
[not_included] [Technical] Граф codemap пересобран из `c00418f` на 0.0.14 (deep, `--repeat 3`): 111 с, 5214 узлов / 14441 ребро, 12 рёбер с `extras.seen`. Подхвачен `reload` без перезапуска; `resolve('config_signature')` → `null`. Сервер MCP этой сессии по-прежнему исполняет код 0.0.12 (`tool_restart_needed`), графу это не мешает
[not_included] [other] Ответ в codemap#18: правило проверено пробой, с нашей стороны issue можно закрывать

[codmap 0.0.15: правило называет себя; два сервера MCP — второй чужой]

[not_included] [Technical] На 0.0.14 зелёная строка `check` не называла `no_type_only_cycles` — правило работало (проверено пробой), но вывод о нём молчал. На 0.0.15 строка читается «Rules enforced: layered (6), forbidden (8), no_cycles, **no_type_only_cycles**, exhaustive», rc 0 на том же `codemap.toml` и том же дереве. Наш зонд остаётся в силе: он проверял поведение, а не текст строки
[not_included] [other] Про «два сервера MCP, второй убить»: по `ppid` второй процесс (со вчера) принадлежит **не этой сессии** — его родитель `claude --resume codemap-bquant`, то есть живая соседняя сессия; наш поднят реконнектом сегодня в 11:18 под `claude --resume bquant`. Убивать чужой процесс не стали: перезапуск за его сессией. Проверка — `ps -o pid,ppid,args`, а не совпадение argv: argv у обоих одинаковый
[not_included] [Technical] Граф пересобран с `9f456b4` уже на 0.0.15 (deep, `--repeat 3`): 115 с, 5214 узлов / 14440 рёбер, 3 ребра с `extras.seen` (против 12 на прошлой сборке — разброс сэмплов, не изменение дерева). Подхвачен `reload`, `tool_restart_needed` больше нет: сервер после реконнекта исполняет установленный код

[Релиз 0.0.15]

[included] [Changed] pyproject.toml, bquant/__init__.py, uv.lock — версия 0.0.14 → 0.0.15; CHANGELOG.md — `[Unreleased]` → `[0.0.15] - 2026-09-07`: вводный абзац, отдельная строка про неадаптивный путь, «Ломающие изменения одним списком», «Известные ограничения» (остатки G40 и G47, три отклонённых предложения аудита, 9 ленивых циклов импортов вне гейта)
[not_included] [Technical] Гейт содержимого = гейт на `c00418f` (3470/34/0 в обоих плечах, батарея 36/36, контракт на codmap 0.0.15); релизный коммит меняет только версию, CHANGELOG и `uv.lock` — повторно не гонялся
[not_included] [Technical] Релиз опубликован: коммит `e1d9bc3`, тег `v0.0.15` на обоих зеркалах (сверено `ls-remote`), https://pypi.org/project/bquant/0.0.15/ — wheel + sdist, `twine check` PASSED. Первый ответ версионного эндпоинта показал только wheel — кэш индекса; повтор показал оба файла. Установка в чистом venv 3.12 прошла со второй попытки («unsatisfiable» на первой — тот же кэш)
[not_included] [Technical] Установочная проверка из нейтрального cwd, импорт из site-packages, по строке на ломающую правку: `config_signature` отсутствует, `CACHE_VERSION` 27, `ZONE_SCALE_K` {'find_peaks': 0.3, 'pivot_points': 0.25}, адаптивное покрытие 65 и 63 из 77 (совпадает с гейтом), неадаптивный `zigzag` 70, `calculate_global` без `set_zone_scale()` — `ValueError`, `last_thresholds` несёт `min_amplitude_pct`; read-only site-packages — импорт и `bquant list` rc=0, посторонних каталогов нет
[not_included] [other] Лаба уведомлена: kogriv/bquearch#12 (сха `e1d9bc3`; между их сборкой `714b09b` и этой в `bquant/` тронуто 4 файла, +142/−55). По грепу их чекаута живых вызовов адаптивного режима нет — `research/shared/pipeline.py` строит `with_strategies(swing=cfg.get("swing", "zigzag"))`; адаптив встречается в прозе SW1 и в копии нашего же ноутбука. Их пин `>=0.0.14,<0.0.15` эту версию не берёт — решение их. Закрыты как перекрытые bquearch#7 и #8
